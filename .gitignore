.DS_Store
.build/
ios/build/
build/
DerivedData/

# 构建产物：整目录忽略。
# 注意 ios/build-device/ 里曾存在**过期**的 .app（Info.plist 缺 NSBonjourServices、
# 语音权限键与 CFBundleIconName），一旦被当成当前配置会对合规判断产生误导。
build*/
ios/build*/
*.xcresult

# Xcode user state
ios/BrewPing.xcodeproj/project.xcworkspace/xcuserdata/
ios/BrewPing.xcodeproj/xcuserdata/
*.xcuserstate

# SwiftPM
*.xcode.version-*
.build/**/*.o

# Node
node_modules/

# 构建 / 检查日志（临时产物，不入库）
.build-*.txt
.cargo-test.txt
.tsc-out.txt
.git-*.txt
.commit*.txt
Sources/BrewPingwinDesktop/fbr.txt

# 本地运行时日志（dev server 持有，删除会失败）
.workbuddy/tauri-dev*.log


# notarized release artifacts
dist/
