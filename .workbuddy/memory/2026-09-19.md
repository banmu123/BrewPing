# 2026-09-19

## macOS Desktop 1.0.0 发布：DMG 打包 + 公证 + 上传 Release

### 目标
把 Mac 桌面端 1.0.0 打到 GitHub Release v1.0.0，分 Apple Silicon / Intel 两版。

### 真正卡住的地方（两个真 bug，都不是权限问题）
1. **`hdiutil create -format` 只能配 `-srcfolder`/`-srcdevice`** —— 单独用来建空白镜像必报 usage。
   9-17 我据此误判成「/Volumes 不可写 = 缺完全磁盘访问(FDA)」，绕了一大圈。
   ✅ 空白镜像正确写法：`hdiutil create -size 200m -fs HFS+ -volname "X" raw.dmg`（**不带 -format**）。
2. **DMG 本身也要 Developer ID 签名**，只公证不签名 → `spctl` 判
   `rejected / source=no usable signature`。且签名会改内容 → **必须先签名、再公证**。
   顺序：打包 dmg → `codesign --sign ID --timestamp` → `notarytool submit --wait` → `stapler staple`。

### dmgbuild 为何弃用
它把镜像挂到 `/Volumes` 后用 `ditto` 写入；在 FDA 受限环境必失败
（`ditto: ... Operation not permitted`），且**失败时只留下 40KB 空镜像**，极难排查。
✅ 改用：空白镜像 + `hdiutil attach -mountpoint /tmp/xxx`（自定义挂载点，全程不碰 /Volumes）
→ cp app + `ln -s /Applications` → detach → `hdiutil convert -format UDZO`。
代价：无自定义背景图（发布形态仍是 app + Applications 快捷方式，符合需求）。

### 产物（全部实跑验证通过）
| 文件 | 架构 | 大小 | spctl | staple |
|---|---|---|---|---|
| `dist/BrewPing-1.0.0.dmg` | x86_64 arm64 (Universal) | 6.7M | accepted / Notarized Developer ID | ✓ |
| `dist/BrewPing-1.0.0-AppleSilicon.dmg` | arm64 | 3.9M | accepted | ✓ |
| `dist/BrewPing-1.0.0-Intel.dmg` | x86_64 | 4.0M | accepted | ✓ |

签名信息：Bundle ID `com.brewping.desktop` / Team `TGA82PM3DZ` /
`Developer ID Application: Zhenkang Chen` / Hardened Runtime (flags=0x10000 runtime) /
notary profile `BrewPingNotary`（--wait 约 35-40s 即 Accepted）。

单架构变体做法：复用已签名的 Universal app，`lipo -thin <arch>` 抽出 → 重签 app → 打 dmg →
签 dmg → 公证 → staple。**不需要重新编译**，1 分半搞定两个变体。

### 旧包不能用的原因（重要）
`build/BrewPing-1.0.0.dmg`（9-15）与 `dist/BrewPing-1.0.0-Intel.dmg`（9-16）虽然已公证，
但 Bundle ID 是 **`local.brewping.desktop`**（当前代码是 `com.brewping.desktop`），
且 9-15 后桌面端改过 3 次（Bonjour 发布回调 / 死代码清理 / 合规修复）→ 不代表当前代码。

### 已上传 GitHub Release v1.0.0（6 个资产）
视频 + 2 个 Windows 包 + 3 个 Mac DMG（上传名带 -Universal/-AppleSilicon/-Intel），
正文加了 Mac 下载表（含 SHA-256），三个直链免登录 HTTP 200 实测可下载。

### 桌面端下载入口改到 GitHub Release
iOS `ContentView.swift:391` 与 Android `HomeScreen.kt:506` 的「Download Desktop App」按钮，
原指向 `https://www.commitbrew.com/#download`，统一改为
`https://github.com/banmu123/BrewPing/releases`（安装包以 Release 为准，官网仅作介绍页）。
iOS 源码现只剩两个外链：Release 下载 + 隐私政策 `banmu123.github.io/BrewPing/privacy.html`。

⚠️ **官网下载页已滞后（待办，未处理）**：`/download/brewping` 只挂了
`/install/BrewPing-1.0.0.dmg`（5,808,162 bytes，与本地三个包 7,040,190 / 4,115,988 /
4,210,684 **都对不上**，来源不明）与 `BrewPing_0.1.0_x64-setup.exe`（Windows 仍是 0.1.0）。
下次应把官网下载页同步到新产物或改为跳 GitHub Release。

### 脚本改动
- `build-dmg.sh`：dmg 打包改 hdiutil 自定义挂载点方案；新增「签名 .dmg」阶段（公证前）
- `Scripts/build-mac-app.sh`：NOTARY_PROFILE 默认 `brewping-notary` → `BrewPingNotary`

## Android 补齐 iOS 对齐短板（P0 两项）

背景与逐项对照见 `docs/BrewPing-Android-iOS-功能对齐审查.md`（同日产出）。

### ① 新增 Help / About 页（对齐 iOS `HelpView.swift`）

- 新增 `Android/app/src/main/java/com/brewping/android/BrewPingConfig.kt`：对外常量集中处，与
  `ios/BrewPing/BrewPingConfig.swift` **一一对应**（隐私政策 URL / 支持邮箱 / 桌面端产品名 / displayVersion）。
  版本号刻意不用 `BuildConfig` —— AGP 8 起 `buildConfig` 默认关闭，读 `PackageManager` 更稳且不必改构建配置。
- 新增 `Android/.../ui/HelpScreen.kt`：使用说明 / 4 步配置 / 隐私政策链接 / 支持邮箱 / 商标免责 / 版本。
- `HomeScreen.kt` 顶栏加「帮助与关于」文字按钮（与既有 `LanguageMenuButton` 同风格）+ Help 浮层。
- 中英 `strings.xml` 各 +19 条（各 120 条，key 集合完全一致）。
- 与 iOS 的**有意差异**（已写进 HelpScreen 的 KDoc）：语言切换不放在这里（Android 顶栏已有
  `LanguageMenuButton`，功能等价、只是位置不同）；暂无「添加演示设备」段（Demo 模式属 P1 未做）。

### ② 配对令牌改 Keystore 加密（原为**明文** SharedPreferences）

- `store/PairingStore.kt` 重写：密文存 SP，密钥在 **Android Keystore**（AES-256/GCM，
  `KeyGenParameterSpec` + `GCMParameterSpec(128, iv)`，iv 随密文存，格式 `v1:<b64 iv>:<b64 ct>`）。
- 选 Keystore 而**不是** `EncryptedSharedPreferences`：后者要新增 AndroidX 依赖，而这里只需加密一个
  短字符串 → **零新依赖**（本机无 JDK 编不了，加依赖的风险更高）。
- **明文透明迁移**：旧数据没有 `v1:` 前缀 → 当明文返回并加密回写，用户无需重新配对。
- 解不开（换机后从云端备份恢复，密钥已随旧设备丢失）→ 返回 null → 走既有「需要重新配对」路径。
- Keystore 异常时退回明文（与改造前一致，不阻断配对）；正常设备不会走到。
- 四个公开方法签名不变（`token` / `isPaired` / `saveToken` / `clearToken`），调用方零改动。

### 验证（本机无 JDK，**编不了 Kotlin**）

Android 侧做了静态核对：
1. `strings.xml` XML 合法 + 中英各 120 条、key 集合**完全一致**（无孤儿键）；
2. `HelpScreen` 引用的 20 个 `R.string.*` 全部存在；
3. 状态机括号/引号平衡校验，4 个改动文件全部通过；
4. `git diff` 确认 `HomeScreen.kt` 只有 3 处**纯新增**（+14/−0）、`PairingStore` 公开 API 未变。

⚠️ 待办：Android Studio / CI 跑 `./gradlew assembleDebug` + 单测；**Keystore 那条路径只能在真机/模拟器
验证**，JVM 单测覆盖不到（现有 6 个单测都不涉及 PairingStore）。

### 未做（后续）

- P1：Demo 模式、清理 `CommandReceiver.kt` 死桩（生产代码从未调用）。
- 顺手发现：`HomeScreen.kt` 里仍有硬编码英文（如 `title = "Add Device"`），未走 strings，i18n 有缺口。

## Android：targetSdk / compileSdk 升到 36（Android 16）

### 🔧 更正上文两处错误结论

1. **「本机无 JDK，编不了 Kotlin」是错的** —— 本机有 **JDK 17**（`D:\study\java\devlop\jdk17`），
   默认 `JAVA_HOME` 指向的是 jdk25（对 Gradle 8.14 偏新），显式指定 17 即可构建。
   Android SDK 在 `D:\software\androidSDK`，`build-tools` 已有 35.0.0 与 36.0.0。
   **Android 侧一直都能真编译，之前几轮用静态核对代替编译是没必要的妥协。**
2. 「现有 6 个单测」也是错的 —— 实际 **83 个**（`testDebugUnitTest` + `testReleaseUnitTest` 各跑一遍 = 166）。

### 改动（4 个文件）

- `Android/app/build.gradle.kts`：`compileSdk 35 → 36`、`targetSdk 35 → 36`，注释写明 Play 期限与版本链。
- `Android/build.gradle.kts`：顶部加「版本链互相绑定」说明（**AGP 8.10 支持的最高 API 级别正好是 36**）。
- `Android/README.md`：`targetSdk: 34`（已过期，与代码都不符）→ 补全 compileSdk/targetSdk = 36、
  版本链表格、本机需装的 SDK 组件，并加 API 37 前瞻。
- `.github/workflows/ci.yml`：android job 新增 `Ensure Android SDK Platform 36` 幂等步骤
  （runner 镜像预装集合会漂移；缺了 AGP 不会降级而是直接失败）。

### 版本链（核实自官方文档，非推测）

`targetSdk 36` → `compileSdk 36` → **AGP ≥ 8.9.0**（AGP 8.10 release notes：*maximum API level = 36*）
→ Gradle ≥ 8.11.1 → JDK 17 → SDK `platforms;android-36`。
本项目 AGP 8.10.0 / Gradle 8.14 / JDK 17 **本来就满足**，只需提 SDK 级别；**要上 API 37 则必须同时升 AGP**。

### Android 16 强制行为 — 逐项核对，本项目均已满足

| 变更 | 核查结果 |
|---|---|
| 边到边强制（`windowOptOutEdgeToEdgeEnforcement` 停用） | ✅ `MainActivity` 已调 `enableEdgeToEdge()`；主题/清单无 opt-out |
| 预测性返回（`onBackPressed`/`KEYCODE_BACK` 不再派发） | ✅ 全仓无覆写；`HomeScreen.kt:171` 用的是 compose `BackHandler`（走 `OnBackPressedDispatcher`，受支持路径） |
| 大屏忽略方向/尺寸限制（≥600dp） | ✅ manifest 无 `screenOrientation`/`resizeableActivity` 声明 |
| 16KB 页 / `scheduleAtFixedRate` / JobScheduler / 健康权限 | ✅ 均不涉及（无 native libs、无相关 API 调用） |

⚠️ **踩坑提醒**：核查 `BackHandler` 时，用 shell `grep` 得到「无命中」是**假阴性**（本机 Git Bash 缺 `grep`，
`cmd || echo "(无)"` 把工具缺失也吞成了「无」）→ 差点误判需迁移预测性返回。**查找一律用 Grep/Glob 工具。**

### 🆕 本机 SDK 补装 `platforms;android-36`（走网络排障）

AGP 会尝试自动下载，但**下载被拦断**：
`Warning: An error occurred while preparing SDK package Android SDK Platform 36: Error on ZipFile unknown archive`。
→ 手动从 `https://dl.google.com/android/repository/platform-36_r02.zip`（65,878,410 B）下载，
用 **Python `urllib` + HTTP `Range` 断点续传**（每轮被掐断但可累积，5 轮拿全），SHA1 从官方
`repository2-3.xml` **现场取值**比对（⚠️ 我第一次把校验值手抄漏了 2 个字符 → 误判「校验失败」，
**别手抄常量，从源头取**）。解包到 `D:\software\androidSDK\platforms\android-36`。
> 与本仓库既有病根同源：本机链路存在 TLS 拦截（见 MEMORY.md「网络间歇性被 TLS 拦截」）。

### 🐞 真编译抓出上一轮 P0 的一个真实编译错误（静态核对没抓到）

```
e: HelpScreen.kt:79:13 / 96:26 / 96:44  This material API is experimental ...
```
`CenterAlignedTopAppBar` + `TopAppBarDefaults.centerAlignedTopAppBarColors` 是 `@ExperimentalMaterial3Api`，
上一轮漏了 `@OptIn`（`HomeScreen.kt:96` 本来就有，照抄即可）。已补 import + 注解。
**教训：括号平衡 / XML / 引用存在性这类静态核对**证明不了能编译。

### 验证（这次是真编译）

- `JAVA_HOME=<jdk17> ./gradlew.bat assembleDebug test` → **BUILD SUCCESSFUL in 2m21s**
  （剩余的 `w:` 全部是**既有**弃用告警：`LocalePrefs` 的 `Locale(String)`、
  `DesktopDiscoveryManager` 的 `NsdManager.resolveService` / `NsdServiceInfo.host`）
- **单测 83 × 2 变体 = 166，failures=0 / errors=0**
- APK 实证（`aapt2 dump badging`）：`compileSdkVersion='36'`、`targetSdkVersion:'36'`、
  `platformBuildVersionName='16'`、`minSdkVersion:'26'`；debug 与 release 的 merged manifest 均为 36

### 前瞻（写进 README，下次升级必须处理）

🚨 **API 37（Android 17）引入 `ACCESS_LOCAL_NETWORK` 运行时权限**（属 `NEARBY_DEVICES` 组），
本地网络**默认对应用封闭** —— NSD/mDNS、局域网 HTTP、**`.local` 域名解析**、
`NsdManager` / OkHttp 全部受影响。Play 将在 **2027-08-31** 强制 API 37。
届时必须：manifest 声明 + 运行时请求 + 处理拒绝/撤销（降级到手动 IP）。
**只改版本号会让自动发现整体失效。** 另外官方给了免权限路径：`NsdManager` 的
`DiscoveryRequest(FLAG_SHOW_PICKER)` 系统选择器 —— 值得评估（隐私更好且不用要宽泛权限）。

改动**未提交**。

## 仓库记忆整理

`MEMORY.md` 已超注入上限被截断 → 重写：**12033 → 8917 字符**（去重/合并、删已解决项、
把取证过程让位给当日日志）。关键 🚨 规则逐条复核未丢；顺带修正「本机无 JDK」为「本机有 JDK 17」。
