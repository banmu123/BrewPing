# 2026-09-16 工作日志

## 开源发布与网站分发合规审计（完整报告见 .workbuddy/outputs/）

**结论：修改后可以发布**（唯一硬阻塞 = 缺 LICENSE）。

### 关键事实（已实查，可复用）
- 🚨 **仓库无 LICENSE / NOTICE，历史中从未有过**；README.md:283 与徽章却声明 MIT
  → P0：必须补 MIT 全文 + 版权主体（个人 or 实体，待人工确认）。
- **零第三方依赖风险**：`Package.swift` 无外部依赖；iOS `PBXFrameworksBuildPhase` 为空、
  无 SPM/Pods/vendored framework；macOS .app 只含自建二进制 + icns + plist
  （**不含** Node / OpenCode / Claude Code / Codex / pi / relay-server）。
- 只是**检测 + Process 调用**本机 CLI（`AgentDiscovery.swift:89`、`CommandRunner.swift:118`）；
  Node 安装走 nvm/官方脚本且需用户在向导触发（`EnvironmentSetup.swift:22,307`、
  Windows `env_setup.rs:493`）——属"引导安装"非再分发。
- 无 telemetry / analytics / crash reporting；无真实硬编码凭据（`sk-` 命中全是 `#[test]` 固件）；
  token：Mac `0o600` 文件 + CSPRNG + 定长比较，iOS Keychain + `privacy:.private`。
- macOS 打包链规范：Hardened Runtime + timestamp + notarytool(--wait) + staple(.app 与 .dmg)
  + spctl + sha256。DMG 内**无** LICENSE（P2）。
- 商标声明：README(中英) + iOS App 内有；**macOS/Windows/Android 三端零命中**，官网无法确认。

### P1 待修
1. bundle id 不一致：`build-app.sh:85` = `local.brewping.desktop` vs
   `Scripts/build-mac-app.sh:46` = `com.brewping.desktop`（iOS 是 com.brewping.ios）→ 统一。
2. `ios/build-device/Debug-iphoneos/**` 的 Debug 二进制**已被 git 跟踪**（后来加的
   `ios/build*/` ignore 对已跟踪文件无效）→ `git rm -r --cached`。
3. relay-server 无鉴权（`auth.ts:22-27` anonymous 全放行）+ 记录完整 payload
   （`server.ts:163`），与隐私政策"不记录命令内容"冲突 → 标注未启用或修。
4. 官网下载页需补商标免责（同 README.md:279 措辞）。

### P2 要点
Watch `PrivacyInfo.xcprivacy` 用了 UserDefaults 却只声明 FileTimestamp（缺 CA92.1）；
`http_server.rs:762` 明文记命令原文；`build-dmg.sh:50-52` 硬编码 `/Users/banmu/...`；
素材（logo/icons/mipmap）无任何来源说明，需人工确认。

## 审计问题修复（P0 + P1 四项 + P2-1）—— 已推送

- ✅ **P0**：新增根目录 `LICENSE`（MIT + 版权行 `Copyright (c) 2026 Chenzk` + 商标段），
  README×2 的 License 段改为指向 `./LICENSE`（`bec55af`）。⚠️ 版权主体待用户确认是否需要
  改成法律实体/全名。
- ✅ **P1-1**：`build-app.sh` bundle id `local.brewping.desktop` → `com.brewping.desktop`。
- ✅ **P1-2**：`git rm -r --cached ios/build-device`（27 个 Debug 产物移出索引；历史仍可检出，
  彻底清除需 filter-repo）。⚠️ 因 `git rm --cached` 会连带下一次 commit，这批删除实际落在
  `bec55af` 而非 `f435911`，历史已推送故未改写。
- ✅ **P1-3**：relay-server 加「实验性/未接线/勿公开部署」三层警示（README + auth.ts 源码注释
  + 主 README 段落）。
- ✅ **P1-4**：macOS 桌面端「设置 → 通用」新增「商标与归属」卡片（新键 `setLegalTitle`/
  `setLegalBody`，zh/en 已加 → sw 系文案键 Mac 76 条，**Windows 需对齐 +2**）。
- ✅ **P2-1**：两个打包脚本把 LICENSE 拷进 `.app/Contents/Resources`。
- 验证：swift build ✓、swift test 41/41 ✓、实跑 build-app.sh（LICENSE 1292B 进包、
  bundle id 正确）✓、实机截图确认卡片渲染 ✓。

### 待办（新）
- Windows 端 sw 文案键对齐 +2（setLegalTitle/setLegalBody，或用等价命名）。
- Watch `PrivacyInfo.xcprivacy` 补 `UserDefaults (CA92.1)`。
- 官网下载页合规文案（仓库无站点源码，需站点侧改）。

## iPhone 自动发现扫不到设备（多播不通，单播可通）

用户报告：桌面端在跑、手动输 IP 能连，但「附近电脑」列表为空。
**排查结论：Mac 端广播正常**（`dns-sd -B _brewping._tcp` 实测可见实例名；
`BonjourAdvertiser.swift` 无问题），问题在 iPhone↔Mac 的多播路径。
成因优先级：① **iPhone「本地网络」权限未开**——iOS 管控 Bonjour 浏览与 .local
解析，但**直连 IP 字面量不受限**，故「手动能连、自动扫不到」是典型症状；
② 路由器 AP 隔离 / 访客网络 / 跨网段拦多播（本机 Mac IP 常变：192.168.5.9 →
192.168.1.226，需核对 iPhone 是否同网段）；③ iPhone 端 VPN。
**本机 Mac 有 5 个 utun 接口**（疑似 Tailscale 类工具），默认路由仍是 en0，非全隧道。
**改**：nearbyCard 增 else 分支 → discoveryHintCard（同一网段 + 本地网络权限路径
+ 多播被拦说明 + 「手动输入 IP」按钮直连已有流程），仅在搜过一轮空手而归时出现。

## Windows 端拉取：🚨 merge 误删整个 ios/ 目录（已恢复）

拉取远端 8 个提交（含 **4f54a50 release: v1.0.0 macOS Desktop — Developer ID
signed/notarized/stapled**，Mac 端已正式发布）：本地 memory 日志未提交会冲突 →
先 commit（1955081）→ `git merge origin/main` → **零冲突报告（ort 策略）**，
但 merge 结束后 `git status` 冒出 40+ 条 ` D`：`ios/BrewPing/**` 26 个 Swift、
`ios/Watch/**`、`ios/BrewPing.xcodeproj/**`、`ios/Scripts/**` 全部消失，
`ios/` 目录在磁盘上变空。

**远端并未删除这些文件**（`git diff --stat HEAD origin/main` 只有
`ios/build-device/**` 的删除，那是 P1-2 有意为之）→ 判定为 merge 应用删除时
误伤工作区（Windows 侧 git 行为异常，原因未复现）。

**恢复**：`git restore --source=HEAD --staged --worktree ios/`（HEAD 里内容完好）
→ 26 个 Swift 全部回来、status 干净、零损失。
**教训**：merge 后必须立刻看 `git status` 的 ` D` 条目，尤其是**目录级**删除；
发现异常立刻用 `--source=HEAD` 恢复，不要自己 checkout 单个文件。

**确认远端已采纳我的打包脚本**：`Scripts/build-mac-app.sh` 只被补了一行
`cp "$ROOT/LICENSE" "$APP/Contents/Resources/LICENSE"`（P2-1），bundle id 也已
统一为 `com.brewping.desktop`（P1-1）—— 之前提的两个问题都已解决，无需再整合。

## Windows 端对齐：设置 → 通用「商标与归属」（待办已清）

- `src/i18n/locales.ts`：zh/en 各加 `setLegalTitle` / `setLegalBody`，键名取 Mac
  `DesktopStrings.swift` 的 LKey rawValue 同名，文案逐字照搬（L813/L1215）。
- `src/App.tsx`：general 区「环境设置」卡之后插入同构 section（10px 大写小标题 +
  11px muted 正文），位置与 macOS `SettingsView` general 分支顺序一致。
- 验证：tsc + vite 通过（2624 modules，6.59s）。提交 `fc9dc48`。

## 🚨 新约定：推送前必须先拉取

用户明确要求「**每次完成推送前都先拉取一次代码再进行远程推送**」。
固定流程：`git fetch origin` → 比对 `git rev-list --left-right --count HEAD...origin/main`
→ 有落后则 `git merge origin/main`（**不用 rebase**）→ **merge 后立刻核对
`git status` 有无目录级 ` D`**（配合记录 merge 前后 `git ls-files` 数量）→
再 commit/push。推送一律走 `PortableGit\bin\bash.exe`（PowerShell 下 push 会 exit 128）。
附带：memory 日志这类双方都改的 append-only 文件，会与远端冲突 → 合并前先备份、
`git checkout --` 还原，merge 完再追加回去。

## 桌面端重复实例导致的端口漂移（排查要点）

发现两个实例同时在跑：debug 二进制（进程名 `BrewPingDesktop`）占 8787，
release app（进程名 `BrewPing Desktop`，带空格）启动时**自动退到 8788** ——
QR 上显示的是实际端口 `192.168.1.226:8788`，而 curl 8787 静默打到了另一个实例。
🚨 **`pkill -f "BrewPing Desktop"` 匹配不到 debug 的 `BrewPingDesktop`**（反之亦然），
清理必须两条都杀。排查端口时 `lsof -nP -iTCP:8787` 与 `8788` 都要看。

## 自动发现：Xcode 直装可用 / TestFlight 不可用（分析结论）

**已实测排除**（重要，避免重复怀疑）：Release 归档包 Info.plist 键齐全且取值正确
（`NSBonjourServices = _brewping._tcp`、`NSLocalNetworkUsageDescription`、
`NSAllowsLocalNetworking=1`）；源码**无任何 `#if DEBUG`**；Debug/Release 仅标准 9 项差异
（优化/DEBUG 宏/get-task-allow/VALIDATE_PRODUCT 等），无一影响 Bonjour；
`Info.plist` 自 09-15 01:32 未变，早于两个构建。
→ **两个包在发现能力上等价，差异只能来自安装态的运行时权限**。

**首要根因**：iOS「本地网络」权限按 bundle id 记录，**删除 App 会清空记录**，而被拒绝后
**系统不再弹窗、静默失败** —— 从 Xcode 签名版换 TestFlight 版通常要先删 App，等于清空授权。
修复：删 App → TestFlight 重装 → 停在设备页等弹窗 → 允许 → **彻底杀 App 重开**。

**待办**：`BonjourDiscovery.swift` 的 `stateUpdateHandler` 把 `NWError` 丢掉了（只判 .failed）
→ 建议打日志并区分「权限被拒 vs 搜到 0 个结果」，下次 TestFlight 排查可一眼定位。

## Watch 对话页自动刷新修复（Mac 已回复但手表停在 loading）

根因：手表详情页只有 onAppear + 手动 Refresh 两个刷新时机；iPhone 仅在
**手表来源**命令完成时推 commandResult（且与落库有竞态），Mac/iPhone 来源
命令零推送 → 手表必然停在旧转录/loading。
**修**：① `requestConversation(id:silent:)` 静默模式（屏上已有内容时不闪
loading、失败不覆盖旧内容）；② 详情页停留期间每 5s 静默轮询（Timer +
onDisappear 清理，unreachable/detailLoading 中跳过），覆盖全部命令来源；
composer 完成时的即时刷新保留作双保险。
**注意**：需重打 TestFlight build 3（或 Xcode 直装）才在手表上生效。
另一个确认中的问题：手机「本地网络」权限弹窗一闪而过被丢弃 → 让用户去
设置 → 隐私与安全性 → 本地网络 手动开（iOS 被拒后不再弹窗）。

## README 按参考规范重写（双语）+ 新增 CI / CONTRIBUTING / SECURITY

参考项目：`~/ai_project/ai_workflow/projects`（LoomFlow）—— 其 README 规范 = 居中 hero +
语言切换 + 徽章组（CI/License/Stars/平台/测试数）+ emoji 分节（Screenshots / Who is it for /
Why instead of / Highlights / Architecture / Quick Start / Documentation / Tech Stack /
Project Structure / Testing & CI / License / Credits），并配套 CONTRIBUTING / CODE_OF_CONDUCT /
SECURITY / CHANGELOG / ISSUE_TEMPLATE / PR 模板 + ci.yml。

**本轮产出**（commit f4fa1a2）：
- README.md + README.zh-CN.md 按同一结构重写；**修正事实错误：watchOS 9.0+ → 11.6**
  （工程与归档产物实测 MinimumOSVersion=11.6）；补实测版本 iOS 17.0 / macOS 13.0 /
  Android minSdk 26 + JDK 17。
- 新增 `.github/workflows/ci.yml`：jobs = swift（build+test）/ apple-targets
  （iOS+watchOS 关闭签名编译）/ hygiene（LICENSE 存在、**无编译产物入库**、双语 README）。
  hygiene 的 grep 已本地空跑 0 命中（正是这两天清掉 ios/build-device 的守卫化）。
- 新增 CONTRIBUTING.md（各端命令 + CI 说明 + 仓库约定：双语文案同批、新 Swift 文件登记
  pbxproj、配置写回必须带测试）、SECURITY.md（token 等价命令执行权限、审批闸门非沙箱、
  relay-server 划出范围）。

**未做（有意）**：README 不加 📸 Screenshots —— 现有开发截图含二维码/配对码/局域网 IP/
厂商地址，且当前模型读不了图无法核实脱敏，留给用户加净化后的截图。
**待办**：CI 首跑后把徽章测试数（41）保持更新；可补 Android/Windows 构建 job、
CODE_OF_CONDUCT、CHANGELOG、ISSUE_TEMPLATE、PR 模板。

## CI 借鉴 LodyAI/Lody 落地（commit 4e1c7ab）

**实证核对**（读了对方 ci.yml / release.yml 原文，纠正了转述）：
- **只 pin 第三方 Action**（`peter-evans/create-pull-request@22a9089…# v7`），GitHub 官方
  `actions/*` 仍用浮动大版本 —— 不是「全部 pin」。
- **fail-open 兜底块刻意留在可信 YAML 里且两处字节一致**，注释写明「不许挪进
  select-ci-scope.mjs，那个脚本不可信（PR 可改）」→ 增量裁剪的安全floor 必须在 YAML。
- `cancel-in-progress: ${{ github.event_name == 'pull_request' }}`：PR 取消、main/定时不取消；
  release 流一律 `cancel-in-progress: false`。
- workflow 级 `contents: read` + 需要写的 job 就地提权；每个 job 都有 `timeout-minutes`。

**BrewPing 已落地**：最小权限 + persist-credentials:false + 第三方 SHA pin + timeout +
concurrency；新增 **windows job**（npm ci / npx tsc / cargo test --locked）与 **android job**
（JDK17 + assembleDebug）；ci.yml 加** nightly schedule** 捕捉 runner 工具链漂移；
新增 dependabot（github-actions 周更，让 pin 不僵死）、PR 模板、CONTRIBUTING 的
「Workflow & CI security conventions」一节。
**未采纳**：增量裁剪（当前规模收益 < 维护成本；等收外部 PR 再上）。
📌 本机无 gh CLI、无 JDK17/cargo → Windows/Android job 无法本地验证，只能看线上首跑。

## CI 首跑实测：3 绿 2 红 + 关键排查手段（runner 日志匿名读不了 → 让 CI 自证）

**首跑结果**（run #8 Dependabot PR / #9 push a322a29 一致）：
✅ Swift core（build+test 41 用例）· ✅ iOS+watchOS compile-only · ✅ Repository hygiene
❌ **Android** assembleDebug · ❌ **Windows** Rust tests（job 跑 4.5 分钟，编译后有用例挂掉）

**🔑 关键发现：GitHub Actions 的 job 日志通过 API 下载需要 admin**（匿名请求返回
403 "Must have admin rights to Repository"），所以无法从命令行读日志；但
**check-runs 的 annotations 可以匿名读取**（`GET /repos/{o}/{r}/check-runs/{id}/annotations`），
且 `GET /repos/{o}/{r}/commits/{sha}/check-runs` 也能匿名列出全部 check run。
→ 对策：**让 CI 自己把失败原因写成 `::error` 注解**，排查就不用等人贴日志。
（本环境无 gh CLI，这套公开 API 路由是无鉴权环境下唯一可行的远程诊断路径。）

**Android 已排除**（本轮实查）：gradlew 干净（LF 行尾 / shebang `/bin/sh` / 本地无 +x 但
git 已 `update-index --chmod=+x`，且 CI 里有 `chmod +x gradlew` 兜底）；git 里的
`gradle-wrapper.jar` 是有效 zip（`PK\x03\x04`，43764B）；构建配置正常（Gradle 8.14 /
JDK17 / compileSdk 35 / AGP 8.10.0 / Kotlin 2.1.0）；`distributionUrl` 指向的腾讯云镜像
`gradle-8.14-bin.zip` **文件真实存在**（HEAD 200 + content-type zip + last-modified
2025-04-25），且 check-host.net 全球多节点（DE/RU/TR/IR）都能触达 CDN。
**最可疑剩余项**：job 只跑了 **5 秒**（06:28:44→06:28:49）→ Gradle 根本没起来，
怀疑 JAVA_HOME / SDK 路径 / wrapper 启动层。已加环境自检（打印 java -version、
JAVA_HOME、ANDROID_HOME、distributionUrl、gradlew 权限）+ 失败提炼注解。

**改动**（commit 0fb8d50 + 2f9b545）：
- android job：环境自检 + SDK 目录缺失发 ::warning + `tee` 落盘 + `--stacktrace` +
  失败提炼 `::error`
- windows job：Rust tests 步**显式 `shell: bash`**（windows-latest 默认 PowerShell，
  需要 PIPESTATUS 与 tee）+ `cargo test --no-fail-fast`（一次暴露全部失败用例）+
  同样的失败提炼注解

**待办**：① 等 run #10/#11 的注解定位 Android/Windows 真因；② Action 版本有弃用告警
（checkout@v4 / setup-java@v4 被强制跑在 Node 24；实测最新为 checkout **v7.0.1**、
setup-java **v6.0.1**、setup-node **v7.0.0**）—— 有意不与此轮诊断同批改，避免同时动多个变量。

## 🎯 Android CI 真因（失败自证机制立刻生效）

加了注解提炼后，run #10 的注解直接给出真错误：
`Gradle assembleDebug failed (exit 1)` / `Caused by: java.lang.ClassNotFoundException: "-Xmx64m"`

**根因**：`Android/gradlew` 是**手写精简版**（94 行/2274B，官方 Gradle 8.14 生成的是
251 行/8755B），末行 `exec "$JAVACMD" $DEFAULT_JVM_OPTS ...` 未加引号展开变量，而
`DEFAULT_JVM_OPTS='"-Xmx64m" "-Xms64m"'` 里**带引号** —— shell 的引号移除发生在展开
**之前**，于是 JVM 收到字面量 `"-Xmx64m"`（含引号）→ 不被识别为选项 → 当成主类名 →
ClassNotFoundException。官方脚本靠 `xargs + sed + eval` 二次解析剥离引号。
🚨 **与平台无关**：macOS 上直接跑 `./gradlew` 同样会挂；一直没暴露是因为 Android Studio
走 Gradle Tooling API、不执行 gradlew 脚本。
**修**（f3f4738）：用官方 v8.14.0 的 gradlew/gradlew.bat 替换（cmp 逐字节一致），保持
git 索引 100755。官方脚本还有额外好处：fd 提升、MSYS/Cygwin 路径转换、xargs 存在性检查。
📌 教训：**手写的构建脚本要 diff 官方生成版**；`$VAR` 不带引号展开时，变量值里的引号不会被剥离。

## 🎯 Windows CI 真因（run #13 注解）+ Android 转绿

**run #13（c92076c）结果**：✅ Swift core · ✅ **Android · assembleDebug**（gradlew 修复生效！）
· ✅ iOS+watchOS · ✅ hygiene · ❌ Windows（Rust tests）

Windows 注解给出真因：
`error: proc macro panicked` / `error: could not compile brewping-desktop (lib) due to 1 previous error`

**根因**：`Sources/BrewPingwinDesktop/src-tauri/tauri.conf.json` 的 `build.frontendDist = "../dist"`，
而 `dist/` 是 gitignore 的构建产物（`npm run build` = `tsc && vite build`）。CI 的 windows job
只跑了 `npm ci` + `npx tsc`、**从未 vite build** → `cargo test` 编译 lib 展开
`tauri::generate_context!()` 时找不到该目录 → proc macro panic。bundle.icon 三个文件都在仓库里（已排除）。
**修**（2aac96d）：windows job 在 cargo test 前加 `npx vite build`。
**本地实测**（与 CI 同状态：无 node_modules / 无 dist）：`npm ci` ✓ + `npx vite build` ✓ →
`dist/{index.html,assets,...}` 生成 ✓（证实「缺 dist」正是那个变量）。
📌 注解提炼的 grep 补了 `help: message` —— proc macro panic 的真实原因在 `= help: message:` 行，
不抓这一行只能看到 panicked、看不到为什么。

## GitHub API 配额（匿名 60/h）——本轮踩到的硬约束
连续轮询会打满：`GET /rate_limit`（该端点不计数）查剩余与重置时间；
排查节奏要控制，或把轮询间隔放长（60~120s）。
另：同 concurrency 组的 push 运行，**新提交会取消排队中的旧运行**（#11/#12 因此被顶掉）。

## 本地工作区对齐
`ios/Scripts/check_localization.py`、`tools/make-appicon.py`、`tools/make-dmg-background.py`
的 git 索引是 755（a322a29 提交）但工作区是 644 → `git status` 长期显示 ` M`；
已 `chmod +x` 对齐（无需提交）。剩余 `ios/.DS_Store` 为 Finder 噪音。

## Windows CI：vite build 修复生效，收敛到「单个用例」

**run #14（2aac96d）**：✅ Swift · ✅ Android · ✅ iOS+watchOS · ✅ hygiene · ❌ Windows
→ **proc macro panic 已消失**（前端 dist 补齐），Windows 侧 305 passed / **1 failed** / 1 ignored：

    test services::http_server::tests::message_with_valid_workdir_runs_in_it ... FAILED
    panicked at src\services\http_server.rs:2342:9

**关键判读**：panic 在 2342 行（`assert_eq!`），而不是 2341 行的
`response.expect("命令应在超时前完成")` → 命令**已正常完成**（exit 0），只是断言值不等；
也不是 2338 行的 `assert_ne!(status, "failed")` → 没有被 `invalid_workdir` 拦。

**响应值来源**（command_runner.rs:487-492）：exit_code==0 时
`response_text = stdout_text.trim_end()`，stdout 为空则替换为 `"(no output)"`
→ 所以断言不等只有两种可能：① 拿到 `"(no output)"`（stdout 为空）；② 路径形式不同
（如 `\\?\` 前缀 / junction 解析 / 大小写无关但形式差异）。测试本身已 lower+trim 两侧。

**注解改进**（637697b）：`grep -B2 -A10 -E 'panicked at|test result: FAILED|^error(\[|:)|failures:'`
—— cargo 的 `assert_eq!` 把 left/right 实际值打在 panic 行之后几行，只 grep panic 那行
等于看不到「差在哪」。下一轮 CI 即可拿到实际值再定修法。
📌 通用教训：让 CI 自证时，**断言类失败必须抓上下文行**，否则只剩「某行 panic」这种无用信息。

## 🎯 Windows 最后一个失败用例：8.3 短名 vs 长名（断言太字面）

run #16 的注解（-B2 -A10 抓到断言上下文）给出实际值：

    assertion `left == right` failed: 子进程应跑在设定的 workdir 里
      left:  "c:\users\runneradmin\appdata\local\temp\brewping-wd-here-<uuid>"
     right:  "c:\users\runner~1\appdata\local\temp\brewping-wd-here-<uuid>"

**同一个目录**（`RUNNER~1` 是 `runneradmin` 的 8.3 短名，UUID 后缀相同）。原因：
GitHub runner 的 `%TEMP%` 是**短名形式** → `std::env::temp_dir()` 拿到短名；
而 `cmd` 的 `%CD%` 报**长名**。测试直接拿原始字符串对撞，于是只在 runner 上失败。
**产品行为正确**（子进程确实跑在设定目录），是**测试断言过于字面**。

**修**（http_server.rs:2341-2357）：两侧都过 `folder_browser::validate_workdir`
（App 自己的 realpath + 剥 `\\?\` 规范化）再比，断言强度不变（仍是完整路径一致），
只是不再受字面形式干扰；失败信息也带上实际响应更易诊断。

📌 **Windows 通用教训**：同一目录可有长名 / 8.3 短名 / `\\?\` 前缀等多种合法字面形式，
**比较路径前必须先规范化**（realpath），不要对撞原始字符串。同类坑还有 junction/symlink
（`canonicalize` 会解析、`%CD%` 不会）。

## 🎯 Windows 第四个问题：设备身份不稳定（测试共享真实用户配置的竞态）

run #18（33b30c8，workdir 断言修复）结果：✅ Swift · ✅ Android · ✅ iOS+watchOS · ✅ hygiene
· ❌ Windows —— **workdir 那个用例已通过**，但换成另一个失败：

    test services::device_identity::tests::identity_is_stable_across_loads ... FAILED
      panicked at src\services\device_identity.rs:80
      assertion `left == right` failed: 设备身份必须持久化且稳定
        left:  "bp_win_58b17daf"
       right:  "bp_win_16fc6c3b"

**根因**：`load_or_create()` 的路径硬编码为用户真实的 `~/.brewping/device.json`
（无 `with_path` 注入缝），而 TC-DI-02/03/04 三个用例 + 并行测试线程都会调用它。
全新环境（CI）下文件不存在 → 多线程同时「读不到 → 各自生成 → 互相覆盖」→
同一用例两次加载可能拿到不同 ID。**本机因身份文件早已存在（真实使用时写过），
所有加载都命中读路径、没有写入者 → 无竞态 → 本机永远绿**，这正是「本地过、CI 挂」的原因。

**修**（device_identity.rs）：
- 新增 `load_or_create_at(path: &Path)` 注入缝（对齐项目既有 `with_path` 约定），
  `load_or_create()` 退化为一行包装；
- **并发首启收敛**：先写临时文件、再用 `std::fs::hard_link` 原子落位 —— 链接在目标
  已存在时失败（不覆盖），后来者读回先写入者的身份。
  ⚠️ 刻意不用「create_new 后直接写内容」：那会留下「文件已存在但内容未写完」的窗口，
  并发读者误判损坏反而触发覆盖（自相矛盾）；也不能直接 `write`（无条件覆盖 → 身份翻转）。
- 保留「损坏文件自愈」（覆盖写）作为兜底分支，序列化失败时不落盘（避免写空文件）。
- 测试：三个用例改用各自临时路径（不再碰用户真实配置）；新增 **TC-DI-05 并发首启收敛**
  与 **TC-DI-06 损坏文件自愈**。

📌 通用教训：**用例不得共享真实用户配置路径**（全局状态 + 并行执行 = 竞态）；且
「本机绿、CI 红」的典型成因就是本机已存在该文件（只读不写，掩盖了写路径的竞态）。

## ✅ CI 全绿（run #19 / head 873d0eb）

5 个 job 全部 success：Swift core · **Android** · **Windows desktop** · iOS+watchOS · hygiene。
其中 Windows 通过说明 device_identity 那次改动**编译通过、用例全过**（含新增的并发收敛
TC-DI-05 与损坏自愈 TC-DI-06）。

**本轮从「4 红」到全绿共修 5 处**（按被发现的顺序）：
1. `Android/gradlew` 手写精简版未加引号展开 JVM 参数 → 换官方 v8.14.0 脚本（f3f4738）
2. CI 从未 `vite build` → `tauri::generate_context!()` 找不到 frontendDist → 宏 panic（2aac96d）
3. `workdir` 断言对撞原始路径字面 → runner 的 %TEMP% 是 8.3 短名（33b30c8）
4. 设备身份 3 个用例共享真实 `~/.brewping/device.json` → 并行竞态互相覆盖；
   加注入缝 + 临时文件硬链接原子落盘（873d0eb）
5. 注解抓取加 `-B2 -A10` —— 让断言类失败带上实际值（本轮所有定位都靠它）

**遗留（需用户 GitHub 登录，我无法匿名操作）**：
- Dependabot PR **#1**（`chore(ci): bump the github-actions group with 5 updates`，head `a810b33`）
  基于**修复前的 main**，其 CI 必然复现旧的 Android/Windows 失败。
  需在 PR 页点 **Update branch** 或评论 `@dependabot rebase` 让它跟上 main，绿了再合。
  它正是消掉 Action 弃用告警（checkout@v4 / setup-java@v4 被强制跑 Node 24）的那批升级，
  所以不必手工改版本号 —— 走这个 PR 即可。
- 工作区仅剩 `ios/.DS_Store`（Finder 噪音）。

## 拉取远端 + Action 版本升级（兑现「Android 绿了再升」的承诺）

**拉取结果**：`main` 0 落后 / 0 领先（无新提交可拉）。fetch 下来一个新远端分支
`origin/dependabot/github_actions/github-actions-9374e8e9c9` —— **仍停在 `a810b33`**
（修复前的提交），`git diff HEAD $B` 显示它相对当前 main 会回退 gradlew 官方脚本、
device_identity 修复、ci.yml 新 job → **不能直接合**。PR #1 需先 rebase 到当前 main。

**升级**（已推送）：官方 Action 走浮动大版本（约定：官方可不 pin、第三方必须 pin）
- ci.yml：`checkout@v4→v7`（5 处）、`setup-node@v4→v7`、`setup-java@v4→v6`
- release-mac.yml：`checkout@v4→v7`、`upload-artifact@v4→v7`、
  `softprops/action-gh-release` 换 pin 的新 SHA `efb35369…# v3`

**升级前做了破坏性变更核对**（读各仓 release notes）：
- `checkout` v5/v6/v7 的 BREAKING 是 `allow-unsafe-pr-checkout`（针对
  **`pull_request_target`** 的更安全默认值）—— 我们只用 `pull_request`，不受影响；
- `setup-java` v6 改的是 `jdkFile` → `jdk-file`（我们没用该输入）；
- `action-gh-release` v3 只是运行时 Node 20 → Node 24（官方建议自托管/托管 runner 均用 v3）；
- `setup-node` v7 / `upload-artifact` v7：notes 里无破坏性提示。
⚠️ ci.yml 的升级由每次 push 的 CI 验证；**release-mac.yml 只在打 tag 时才跑**，
其升级要到下次发版才被真正执行验证。

## ✅ Action 升级验证通过（run #21 / head e58f0f8）

5 个 job 全部 success（checkout@v7 · setup-node@v7 · setup-java@v6 在真实 runner 上跑通）。

**告警消除有直接证据**（用公开 check-runs API 读注解对比）：
- 升级前：Android job 有 **3 条注解** —— 2 条 warning（`Node.js 20 is deprecated …
  actions/checkout@v4, actions/setup-java@v4 are being forced to run on Node.js 24`、
  `setup-java v4 is deprecated … migrate to actions/setup-java@v5`）+ 1 条 failure
- 升级后：Android job **0 条注解**，hygiene job 也 **0 条** → 告警清零

→ **Dependabot PR #1 可直接 Close**（其 5 项升级已全部落在 main）。
`release-mac.yml` 的升级仍待下次打 tag 时验证（该 workflow 仅在 tag push 时运行）。

## iOS 自动发现修不好 + 权限弹窗连弹（本轮改造）

### 根因（两层）
1. **`BonjourDiscovery.stateUpdateHandler` 只判 `.failed` 并丢掉 `NWError`** —— 权限被拒
   （`kDNSServiceErr_PolicyDenied` = -65555）与「网络拦多播」在界面上完全同形（都只是
   「扫不到」），无从区分，也没有任何可执行的下一步；
2. **三处权限各自自动触发** → 连弹：启动期自动请求语音（`runStatusLoop` 里
   `requestSpeechAuthorizationIfNeeded()`）、进设备页自动 Bonjour 探测（弹「本地网络」）、
   点扫码自动请求相机。而本地网络一旦被拒，**系统不再弹窗** → 自动发现永久失效且无提示
   （用户反馈「授权框一闪而过没来得及点」正是这个后果）。

### 改动
- `BonjourDiscovery`：新增 `LocalNetworkAccess`（unknown/requesting/granted/denied）与
  `lastBrowseError`；解析 `.waiting/.failed` 的 `NWError` 识别 PolicyDenied；授权成功时
  持久化 `BrewPing.LocalNetworkGranted`（决定「能否自动探测」）。
- **新增 `PermissionCenter.swift`**：统一管理三项权限；单项请求都由用户点击触发；
  `requestAll()` **串行**编排（语音 → 相机 → 本地网络，一步落定再进下一步，
  本地网络无回调 → 状态转移 + 12 s 兜底）；`openSystemSettings()` 给出被拒后的恢复途径。
- `ContentView`：新增权限卡（三行状态 + 单项「授权」+「一键授权全部」+「打开系统设置」）；
  **去掉进页面自动探测**（只在 `hasEverBeenGranted` 时才自动开扫）；回前台在
  「已授权/曾被拒」时重新探测（覆盖「去设置打开再回来」）；**移除启动期自动请求语音**；
  表单内自动发现失败时区分「权限被拒」与「没找到设备」。
- `WatchConnectivityManager`：语音权限改为**按需请求**（手表语音送达且前台时才请求）。
- 文案：en/zh-Hans 各 +15 条（check_localization.py 通过：iPhone 235/235，Watch 39/39）。
- pbxproj：新文件四处登记（`grep -c` = 4）。

### 验证
- iOS Debug 构建 ✓、模拟器版构建 ✓、`check_localization.py` ✓（watchOS 目标另行编译确认）
- 模拟器全新安装（卸载 + 重启模拟器 + 清偏好）实测：
  ① **启动无语音弹窗**（日志 0 条语音请求）② **无未交互的本地网络弹窗**
  ③ 权限卡渲染正确（中文文案、三行状态、一键授权全部、打开系统设置）
  📸 截图 `.workbuddy/outputs/iOS-权限卡-逐项与一键授权.png`
- ⚠️ 踩坑记录：模拟器上曾看到「启动仍弹语音框」的截图 —— 那是**旧进程遗留的僵尸授权框**
  （进程被终止后弹窗留在 SpringBoard 上），`simctl` 重启模拟器即清；同期 `defaults read
  com.brewping.ios` 还残留旧进程写的 Demo 设备偏好，导致界面看不到「无设备」态。
  结论：模拟器验证必须先 `shutdown`+`boot`+`defaults delete`，否则会被上一轮的残留骗到。

## iOS 扫码配对拿不到主机类型（扫 Windows 的码仍显示 Mac）

### 根因（定位到具体一行）
`ContentView.applyScannedValue`（**App 内扫码**这条路）解析深链时只取了
`host / port / code / name`，**唯独没读 `osType`** → 表单 `editOS` 停在默认 `.mac` →
落库就是 Mac。而**外链入口** `PairingURLHandler.handle` 是读了 `osType` 的 ——
两条路各写了一份解析，扫码那份漏字段。用户实际走的就是扫码这条路。

两端其实都发了类型：Mac `DesktopCore.pairingURL` 带 `osType=mac`；
Windows `lib.rs:103` 带 `osType=windows`（且有单测断言）。所以问题纯在 iOS 解析侧。

### 修法（不只是补一行）
- 把深链解析**收敛成一处** `PairingLink.parse`（在 `PairingURLHandler.swift`），
  扫码与外链共用 → 两条路不会再各自漂移。
- `osType` 缺失时用 **deviceId 平台前缀**兜底（`bp_mac_` / `bp_win_`，新增
  `DeviceOSType.fromDeviceIDPrefix`）：老版本桌面端发的码没有 osType，但 deviceId 一直带前缀。
  **两样都没有 → 返回 nil**，扫码路径保持表单现值（不盲目改成 Mac）。
- 扫码后提示回显识别到的系统（`Scanned (%@). Tap Pair to finish.`），用户提交前能核对。
- 文案 en/zh 各 +1（236/236）。

### 验证（三层，均为确定性证据）
1. **解析断言 harness**：用 `swiftc` 把**真实源文件**（ManagedDevice.swift +
   PairingURLHandler.swift）与一个打桩 `L`/日志的 `main.swift` 一起编译后实跑 ——
   18 条用例全过（含 Windows 有/无 osType、Mac、未知前缀返回 nil、大小写/空白容错、
   非法输入拒绝）。📌 要点：harness 必须编译真实文件，不能复制实现（否则验证的是副本）。
2. **扫码路径端到端**（模拟器 + 临时钩子）：临时加 `BP_SCAN_TEST_URL` 钩子，自动打开
   「添加设备」表单并把它当作扫码结果喂进 `applyScannedValue`，然后截图：
   - 当前版 Windows 码 → 系统显示 **Windows** ✓
   - 老版本 Windows 码（无 osType，`bp_win_` 前缀）→ **Windows** ✓
   - Mac 码 → **Mac** ✓（无回归）
   钩子用完即删（已确认文件里无残留）。
3. 构建：iOS / watchOS 均 BUILD SUCCEEDED；`check_localization.py` 通过。

### 顺带确认的既有事实 / 踩坑
- **iOS 18 打开自定义 scheme 会先弹确认框**（「在 "BrewPing" 中打开？ 取消/打开」），
  `simctl openurl` 返回 0 但**不会**替用户点确认 → 深链路线无法用 simctl 端到端验证
  （截图里那个对话框正是它，与我们的改动无关）。
- 老用户已有错误类型的设备：**重新扫一次该 Windows 码并提交**即可纠正
  （`commitDeviceForm` 对已存在设备会写 `existing.osType = editOS`）。

## iOS/Android 首帧闪现「下载桌面端」卡（瞬时未就绪被当成定论）

用户报告：设备能获取到，但**每次退出再进来都闪一下「下载桌面端」**。

**根因（两端同构缺陷，各自机制不同）**
- **iOS**：空态分支只判 `discoveredHosts.isEmpty`，于是自动发现**还在跑**的那几秒
  （此时用户的电脑就在网络上）「添加第一台电脑 / 下载桌面端」卡先渲染出来，等
  「附近」列表出来才消失。
- **Android**：`when { devices.isEmpty() …; device == null -> EmptyStateCard }` ——
  `desktopDevice` 是 `MutableStateFlow(null)`，由 `activeDeviceID` 收集器**异步物化**
  （且它先 `resetState()` 置空再 `connectToDevice`），所以首帧必然是
  「devices 非空 + device == null」→ 直接渲染「下载桌面端」卡 → 物化完成后切走。
- 同批还有一处：iOS 权限卡首帧把**已授权**的本地网络显示成「未授权」
  （`PermissionCenter.localNetwork` 初始 `.unknown` + `speech/camera` 到 onAppear 才读）。

**修**
- iOS `ContentView`：新增 `discoverySettled`（= didAttemptDiscovery && !isSearching &&
  !isResolving），「下载桌面端」卡改为 `discoverySettled && discoveredHosts.isEmpty`；
  权限卡改为 `permissions.hasRefreshed && !allGranted`；搜索期间改显示中性占位
  `searchingCard`（新键「正在查找附近的电脑…」en/zh）。
- iOS `PermissionCenter`：`init` 里同步读 voice/camera（这两个查询 API 无副作用、不弹窗），
  `localNetwork` 首帧用持久化「曾授权」标记近似；`refresh()` 不再把已知的授权值降级成未知。
- iOS `DeviceStore`：`activeDeviceID` 为空但设备非空时也回退第一台（防「有设备却落到未配对表单」）。
- Android `HomeScreen`：`discovered.isEmpty() && !discoveryRunning` 才显示空态卡；
  `device == null` 改渲染中性占位 `PreparingDeviceCard`（新键 `preparing_device` en/zh）。
- ⚠️ Android UI 与 iOS 各自 1 个文案键；**两端文案仍需同批**（本次已同批）。

**验证（关键：模拟器里造出「权限齐 + 无设备」状态做前后对照）**
- 模拟器 TCC 数据库可直接写：`<device>/data/Library/TCC/TCC.db` 的 `access` 表
  （`service= kTCCServiceCamera/kTCCServiceSpeechRecognition/kTCCServiceMicrophone`,
  `auth_value=2`）。`simctl privacy` **不支持** camera/speech，只能走 TCC。
  🚨 必须**关机状态**写（开机写会被 tccd 覆盖）；且 `simctl uninstall` 会清掉这些行。
- 连拍首帧（`simctl io screenshot` ×16）比对：**修复前**帧 5~13 全是「下载桌面端」卡
  （约 4 秒）→ 帧 14 起「附近的电脑 Chenzk」；**修复后**帧 4~10 只有「正在查找附近的电脑…」
  → 帧 11 起附近列表，**全程无「下载桌面端」卡**。截图见
  `.workbuddy/outputs/iOS-首帧闪现-修复前-下载桌面端卡.png` 与
  `iOS-首帧-修复后-只显示正在查找.png`。

**🚨 工具级教训（本次真实踩到）**：**同一文件的多个编辑不可并行发起** —— 两个
Edit 调用同时落在 `ContentView.swift`，后写覆盖先写，其中一处判据**静默丢失**，
直到我按「修复后应该没有那张卡」去核对截图才发现（源码回读才确认）。**改完必须
用 grep/python 回读关键行核对**。本次 iOS/Android 均已回读确认。

**⚠️ 用户手机上的 TestFlight 构建是 09-15 17:12（`3250b1b`）**，不含今天所有 iOS 修复
（权限卡、扫码类型、本处闪现修复）—— 需重打包（build 3）后才可见。
