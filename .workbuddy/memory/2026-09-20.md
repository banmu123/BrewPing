# 2026-09-20

## 拉取合并 + 推送（Windows 侧）

### 过程

- `fetch` 三轮全部失败（`CRYPT_E_NO_REVOCATION_CHECK` / openssl `20`）→ 用**只读**兜底
  `-c http.sslBackend=openssl -c http.sslVerify=false` 拉取成功，远端领先 2（Mac 的 dmg 打包 + 下载入口改 Release）。
- 按纪律**先提交本地改动再 merge**，分主题 4 个提交：
  `a7e145b` Android API 36 配置 → `dacdf37` Android P0（Help 页 + Keystore）→ `54d85df` 对齐审查报告 →
  `81c19b6` merge → `16a41d8` 记忆更新。
- merge 用 **ort 策略，无冲突**（`HomeScreen.kt` 自动合并：我的 Help 按钮 + Mac 的下载地址修复各自保留）。
  完整性核对：`ls-files` 454 → **455**（远端 +1 文件）、**零目录级删除**、23 个顶层目录完好。
- 推送：普通 `git push` 连试 3 次全失败；改 `-c http.sslBackend=schannel push` **第 1 次即成功**
  （`ls-remote` 也验证过同一现象）→ **拦截是间歇性的，schannel 重试往往能过，不必一上来就关校验。**

### ⚠️ 记忆文件的三方冲突处理（重要，下次照做）

本次远端与本机同时改了 3 个文件，处理方式记录如下：

1. **`.workbuddy/memory/2026-09-19.md`**：本机是 untracked 的同名新文件，远端也新增了同名文件
   → **untracked 文件会直接挡住 merge**。做法：先移开本地文件（另存备份）→ merge → 把两侧内容
   **合并写回**（远端 Mac 的 dmg 记录在前、本机的 Android 记录在后，共 182 行）。
2. **`.workbuddy/memory/MEMORY.md`**：本机整篇重写过，直接 merge 必冲突。做法：备份 → `git checkout --`
   还原到 HEAD → merge（远端新增内容干净落地）→ 在**合并后的版本**上重新套用我的精简，并**完整保留
   Mac 新增的 dmg 经验**（hdiutil 参数、dmg 需自身签名且先签后公证、dmgbuild 弃用原因、lipo 抽架构、
   BrewPingNotary、release-mac.yml 缺 secrets）。
3. **`Android/.../HomeScreen.kt`**：改动在不同 hunk，ort 自动合并成功（无需人工）。

### 遗留

- `MEMORY.md` 仍是 **10336 字符**，高于注入截断阈值（约 8400）→ 下次会把尾部（工具与协作约定）截掉。
  今天已从 12033 压到 10336 并吸收了 Mac 新增的 ~1400 字符（等效删除约 3100 字符旧内容）。
  **若要彻底解决，需再砍约 2000 字符，或把取证类细节拆到同目录的伙伴文件里（MEMORY.md 只留索引）。**
- `tools/dmgbuild-settings.py` 仍在仓库里，但 Mac 已弃用 dmgbuild —— 疑似残留，**未动**（不确定就别删）。
