# 2026-09-13 工作日志

## macOS 桌面端：composer 输入框无法输入 + 高度与 Windows 不一致（已修）

**现象（用户报）**：① 对话输入框点进去打不了字；② 输入框高度和 Windows 桌面端不一致；③ 要求布局在窗口尺寸变化时仍正确适配。

**根因（三条独立问题）**

1. **打不了字 —— `NSScrollView()` + `NSTextView(frame: .zero)` 手搓构造。**
   第一版自己 `let scrollView = NSScrollView()` + `let textView = SubmitTextView()`（无 frame）再 `scrollView.documentView = textView`。
   `documentView` 的 frame 是 `.zero`，AppKit **不会**替它布局（`autoresizingMask = [.width]` 只管宽度），文本视图既不可见也不可点击 → 「点不进、打不了字」。
   **修法**：一律用 `NSTextView.scrollableTextView()` 构造（它会把 textContainer / minSize / maxSize / autoresizingMask / widthTracksTextView 全套装好），然后自己接管高度。
   同时把「Enter 发送」从 `keyDown` 覆写改成 `NSTextViewDelegate.textView(_:doCommandBy:)` —— 后者是 AppKit 的标准拦截点，**输入法组字期间不会被调用**，不会吞掉中文候选词的回车。

2. **高度不对 —— `.frame(minHeight:maxHeight:)` 是弹性框，不是钳制框。**
   我写了 `.frame(minHeight: 72, maxHeight: 176)`。SwiftUI 的 frame 在同时给 min/max 时是**弹性**的：父级有余量就一路顶到 maxHeight，并把内容**垂直居中** → 输入框永远 176 高、文字浮在中间（第一版截图就是这样）。
   **修法**：把高度范围钳在 `NSViewRepresentable.sizeThatFits` 里（内容 52…156），外壳只留 padding，再补 `.fixedSize(horizontal: false, vertical: true)` 禁止父级拉伸。**别在 SwiftUI 侧写 min/max frame 去控高度。**
   `max-h-44` 超出后的滚动由 NSScrollView 承担：`textView.frame.height` 用**未钳制**的自然高度（这样 document 比 clip 高，才真的滚得起来）。

3. **尺寸对齐**：Windows 是 `<textarea min-h-[72px] max-h-44 px-4 pt-3.5 pb-1.5 font-mono text-sm>`，且 Tailwind 是 `box-sizing: border-box` → **72/176 包含 14+6 的上下内边距**，内容区 = 52…156。水平 16pt 走 SwiftUI padding（NSTextView 的 `textContainerInset` 只能给上下对称值，表达不了 14/6 的不对称）。
   实测：空态 72pt，6 行文本涨到约 117pt，符合预期。

**布局自适应（第三条要求）**
- 新增 `EnvironmentValues.viewportWidth`（`LatteTheme.swift`），由 `DesktopRootView` 里**唯一一个** GeometryReader 注入（只包主区，不包侧栏）。
- `ConversationColumn` 用它算 `列宽 = min(736, 可用)` 与 `留白 = 可用 < 640 ? 12 : 16`（对齐 `max-w-[46rem] px-3 sm:px-4`）。
- **不要**在会话内部逐处用 GeometryReader：它会把 VStack 的剩余高度全吃掉，composer 会被顶到奇怪的位置。
- 原先的 `ConversationColumnSimple` 用 `.frame(maxWidth: 736) + .padding(.horizontal, 16)` → 实际占 768pt 且窄窗不收缩，已删除。
- 实测：窗口 1080 → 列 736/留白 16；窗口缩到 560（主区约 340）→ 列收缩到主区宽、留白 12、下拉与目录条按 maxWidth 截断、无溢出。

**验证手段（无辅助访问权限时怎么验「能输入」）**
- `osascript` System Events 点击在本机不可用（`-25211`）。
- 改用：临时加一个 `weak var` 全局引用记下 NSTextView，`onAppear` 里若环境变量存在则
  `window.makeFirstResponder(tv)` + `tv.insertText(多行文本, replacementRange: NSRange(location: 0, length: 0))`。
  **`insertText` 就是 NSTextView 处理可打印按键时走的同一个人入口** → 能插进去就说明输入链路通；同时多行文本能验证高度是否随内容增长。
- 窗口尺寸用 `NSApp.windows.first{...}?.setContentSize(...)` 驱动（`.defaultSize` 只在首次创建窗口生效，之后 frame 是从上次会话恢复的）。
- 收尾：`grep -rn "ComposerDebug\|BREWPING_TYPE_TEST\|BREWPING_WIN_W"` 确认零残留。
- ⚠️ **python 改 Swift 字符串字面量时别用 `re.subn` 的 replacement**：replacement 里的 `\n` 会被当成转义写进文件，把 `"...\n..."` 变成真实换行 → Swift 报 `unterminated string literal`。用 lambda 或 `str.replace`。
- ⚠️ 批量改同一文件时，锚点要用「当前文件真实内容」，别用记忆里的内容 —— 中途插入过 `.onAppear` 块，锚点就失效了（`assert count == 1` 挡住了）。

**文件**：`Sources/BrewPingDesktop/ChatView.swift`（`ConversationColumn` / `ComposerInput` / `ComposerTextView` 全部重写）、`Sources/BrewPingDesktop/LatteTheme.swift`（`columnWidth`/`columnGutter` + `viewportWidth` 环境键）、`Sources/BrewPingDesktop/DesktopRootView.swift`（注入 viewportWidth）。

## iOS 端「未连接设备」空状态重构（对齐 Android EmptyStateCard）

**背景**：用户反馈 iOS 未连接状态的界面「冗余太多」。参照刚改好的 Android `EmptyStateCard`（`ab1dbbd`）重做。

**Android 参考样式**（`ui/HomeScreen.kt` 的 `EmptyStateCard`）：一张居中卡片 —— ☕ 32sp / 标题 15sp medium / 一句说明 12sp / 一个主按钮（下载桌面端）/ 一行 10sp 脚注。卡片 16dp 圆角 + 1dp 描边 + 24dp 内边距。`device == null` 时**只**渲染这一张卡，自动发现列表只在有设备分支里出现。

**iOS 改前的冗余**（`ContentView.emptyStateCard`）：一个 Form 里三段 —— ① "Get started"（大段说明 + 4 步编号引导 + 下载按钮 + 脚注）、② "Macs on this Wi-Fi"（扫描指示 + 主机列表 + 「未找到」占位 + footer 长提示）、③ 无标题 Section（Add Manually + Try Demo Mode + 脚注）。同一件事说三四遍，且「Add Manually」与设备栏的「Add Device」重复。

**改后**（`ios/BrewPing/ContentView.swift`）：
- 无设备分支**不再用 Form**，改为 `ScrollView { emptyStateCard + nearbyCard }`，与 Android 同构的单卡片（☕ / "Add your first computer" / 一句说明 / Download Desktop App / 脚注）。
- `discoveredMacsSection` → `nearbyCard`：**只在真的扫到主机时才出现**，去掉 header/footer 与「未找到」占位。Bonjour 自动发现能力保留。
- 删除 `getStartedSection` / `quickActionsSection` / `guideStep`。Demo 入口从空状态移除（HelpView 与设备表单里仍有 `addDemoDevice()`，不会丢功能）。
- 「已添加但未配对」分支保持原来的 Form（`notPairedCard` + `statusBanner`），本身已经很精简，不动。
- `.onAppear { bonjour.startSearching() }` 从被删的 emptyStateCard 挪到新的 ScrollView 上。

**本地化**（`en.lproj` / `zh-Hans.lproj`）：
- 新增 3 key：`Add your first computer` / `Run BrewPing on your computer, then tap “Add Device” to send commands from your phone.` / `Nearby`。
- 删除 16 条空状态旧文案（含本来就没人引用的 6 条），并清掉因此变空的 `/* MARK: - Pairing v2: QR + Auto Discover on empty state */` 小节标题（中英各一处）。
- `python3 ios/Scripts/check_localization.py` → en=214 / zh=214，**无崩溃风险，无漏翻**。

**⚠️ 本机只有 Command Line Tools（无 Xcode / iOS SDK）**，iOS target 无法真编译。用 `xcrun swiftc -parse -target arm64-apple-macosx13.0 ContentView.swift` 做了语法解析校验（exit 0 = 语法无误），类型检查与真机/模拟器验证仍需在有 Xcode 的机器上做。
## 空状态下载引导（09:00）

- EmptyStateCard 加「下载桌面端」主按钮（LattePrimary 实心 + Download 图标，12dp 圆角）+ 底部辅助文案；点击 ACTION_VIEW 打开 https://www.commitbrew.com/#download，无浏览器 ActivityNotFoundException 静默。
- 新增 key：download_desktop / download_hint（双语）。

## iOS 空状态下载引导（09:15）

- ContentView getStartedSection 步骤后加 borderedProminent 大按钮「Download Desktop App」（arrow.down.circle.fill）→ openURL(commitbrew.com/#download)，附 caption2 辅助文案；@Environment(.openURL) 注入。
- 两份 Localizable.strings 加 2 个 key（en 227 / zh 227 对齐）。
- check_localization.py ✅ 无崩溃风险。

## README 重写（Lody 风格，10:00）

- 参照 D:////study////lody////Lody////Lody 的 README.md + README.zh-CN.md 结构：徽章行 → 图标+居中标题 → 语言切换 → 标语/副标题 → 链接行 → 徽章行2 → 图/架构 → 场景三小节 → 连接机器 → CLI → 控制 → 工作区 → More built in → 现状 → 构建 → 仓库结构 → 商标 → License。
- 输出 README.md（English）+ README.zh-CN.md（简体中文），顶部 HTML <a> 互跳（与 Lody 同式），15 个 H2 逐条对齐、代码块数一致。
- 内容全部取自项目实际：4 个 agent（opencode/claude/codex/aider）、真实 CLI 命令、HTTPAPI 路由表、~/.brewping 配置、三端构建命令、授权三档、对话/工作目录；relay-server 明确标注未接入。

## iOS ↔ macOS 消息链路三连修（2026-09-13 11:00）

**用户报了三个问题，逐项排查后发现 ① 和 ② 是同一个根因。**

### 根因 1（问题① + ②）：`CommandRunner.runHeadless` 没走 `finish(...)`

`runHeadless` 只写了 `CommandStore`（`store.update`），而 **`finish(...)` 是唯一把命令终态回写对话转录、并清空 `ConversationStore.latestCommandId` 的地方**。
headless 型 Agent（claude-code / codex / aider —— 即除 opencode 外的全部）都走这条路，所以：
- **助手回复永远不进转录** → iOS 端现有对话看不到回复（iOS 的 `items` 来自 `store.detail.messages`，`.completed` 阶段的文本只在草稿态才本地补显）；
- **`latestCommandId` 悬着不放** → 桌面端 `isBusy = latestCommandId != nil || terminal.status == .running` 永真 → 卡在「思考中」。

opencode 走 `run → watch → finish`，所以这条 bug 一直没暴露。
**修法**：`runHeadless` 改为调 `finish(...)`；`finish` 补 `failureReason` 参数（原来手写 `store.update` 时有设，不能丢 —— iOS 靠它渲染失败原因）。
**验证**：起 debug 桌面端 → `POST /api/message`（带 Bearer + Timestamp + Nonce）→ 轮询 `/api/message/{id}` 到 `completed`（回复 `pong`）→ `GET /api/conversations` 里该对话 `messageCount=2`、`latestCommandId=null` ✅

### 根因 2（问题② 的手动停止）：headless 子进程没有可终止的句柄

`HeadlessCLIAgent.execute` → `SystemCommand.run` 内部 `Process` 是局部的，外部拿不到。原设计只有 600s 超时兜底。
**修法**：
- `SystemCommand.run(..., onLaunch: ((Process) -> Void)? = nil)` —— 子进程真正启动后回调一次；
- `HeadlessCLIAgent.execute(_:workdir:onLaunch:)` 透传（旧签名保留，委托过去）；
- `CommandRunner` 加 `runningProcesses: [String: Process]` + `stopRequested: Set<String>`（`stateLock` 保护）与 `requestStop(commandId:)`：SIGTERM，3s 后仍活着再 SIGKILL；终态时若 `stopRequested` 命中则错误文案改写为 `Stopped by user.`；
- `CommandRouter.stop(commandId:)` → `DesktopCommands.stopActiveCommand()`（取当前对话的 `latestCommandId`，顺带复位终端指示灯）→ `DesktopAppState.stopGeneration()`；
- **UI**：composer 右侧按钮在 `isBusy` 时由发送钮切换为**停止钮**（`stop.fill`，destructive 底），文案 `bar.stop`（zh「停止生成」/ en "Stop generating"）—— key 已同步加进 `locales.ts` 与 `DesktopStrings.swift`，两端仍对齐（148+1）。
**验证**：临时加环境变量钩子（提交一条长任务 → 10s 后调 stop → 再读转录）：`stop returned true`，转录 `roles=user,error`、`latest=nil`、error 条目文本 = `Stopped by user.` ✅（测完钩子已还原）

### 根因 3（问题③）：composer 的高度驱动方式有布局缺陷

`sizeThatFits` 在 SwiftUI 求**理想尺寸**时拿到 `ProposedViewSize.unspecified`（width = nil），回落到 `nsView.bounds.width` —— 首帧是 0，量出来退化成 **1pt 宽**，配合 `.fixedSize` 可能把输入框塌没（点不进、打不了字的另一个来源）；且在 `sizeThatFits` 里反复改 `textView.frame` 会在布局期间再触发布局，容易回环。
**修法**：**彻底不用 `sizeThatFits`**。`ComposerTextView` 增加 `onHeightChange` 回调，AppKit 侧 `reportHeight()`（`layoutManager.ensureLayout` + `usedRect`，**不改 frame 来测量**）把内容高度报给 SwiftUI 的 `@State contentHeight`，外壳显式 `.frame(height: contentHeight + 20)`（52…156 内容 + 20 内边距 = 72…176）。`reportHeight` 只在变化 >0.5pt 时上报，避免循环。
**验证**：`makeFirstResponder` 成功（日志 `firstResponder is tv = true`）+ `insertText` 六行 → 输入框随内容长高、文字贴顶、发送钮转为可用态 ✅

### ⚠️ 测试环境的坑（浪费了几轮）

- **后台进程跨工具调用会被回收**：`(app &)` 起的桌面端在**下一个 Bash 工具调用里已经死了**（`pgrep` 为空、端口拒绝）。端到端验证必须**在同一条命令里**完成「启动 → 提交 → 轮询 → 断言」。
- **代理**：本 shell 有 HTTP_PROXY，访问 `192.168.3.94` 会被代理吃掉（报 `upstream connect failed`）。curl 要加 `--noproxy '*'`，python 要 `ProxyHandler({})`。
- **POST 鉴权**：`/api/message` 需要 `X-BrewPing-Timestamp` + `X-BrewPing-Nonce`（GET 只要 Bearer）。

### 顺手修的数据

`runHeadless` 这条 bug 在用户真实数据里留下了两条**永久卡「思考中」**的对话（只有 user 消息、`latestCommandId` 悬着）。已直接改 `~/.brewping/conversations/conv_*.json` 把 `latestCommandId` 置 null 并重写 `index.json`；停止测试产生的对话已删除。

### 遗留

- iOS 端没有「停止生成」入口（本次只做了 macOS）。若要加：需新增 HTTP 路由（如 `POST /api/conversations/{id}/stop`）**并同步 `DemoBackend.swift`**。
- iPhone 真机链路未实测（本机无 Xcode / iOS SDK），但 HTTP 层与 iOS 走的是同一套接口，行为应当一致。
## 中国区 App Store 合规咨询（10:53）

- 结论要点：纯局域网/无服务器 ≠ 免备案。网站 ICP 备案（需域名+境内服务器）用不上；但中国大陆 App Store 上架需在 App Store Connect 填工信部备案号（苹果官方：App 信息里的 ICP 编号须与工信部一致，且简体中文 App 信息要与备案一致）。
- 路径：走「移动互联网应用程序信息服务备案」（App 备案，beian.miit.gov.cn 或接入商代备案），不要求域名/服务器；主体须与 Apple Developer 账号主体同名。
- 待办（若推进上架）：隐私政策公网 URL、ATS 本地网络例外 NSAllowsLocalNetworking、审核备注 + Demo 模式、无数据收集申报。

## 上架策略（11:57）

- 用户决定：暂不推进中国大陆区上架（App 备案先搁置），优先海外区/其他开发事项。海外区无需 ICP 备案号。

## 三端消息链路排查与修复（12:29-13:05）

**任务**：Android / Windows / macOS 三端消息收发排查，统一契约。

**上游遗留（上一轮会话）已接手完成**：Windows http_server（exec_lock 串行、自动建对话、404 终态、deny 落转录、approve 回归属对话）、approval_gate（check_with 带 conversation_id + Resolution 回传）、command_runner（pump_pipe UTF-8 边界）、conversation_api（建对话门卫前置 + submitStatus 契约）；macOS 四文件（转录 id、HTTPAPI 契约、deny 落转录、HTTPServer 8MB 上限）。

**本轮新增修复**
1. approval_gate 测试 3 处旧签名 + 新增 TC-AG-16（pending 记住 conversation_id）；TC-HT-09 改断言 404 终态；TC-HT-22 改断言批准后 200+commandId。
2. conversation_api.rs 建对话 pending 响应补 approval 对象（对齐 macOS HTTPAPI，客户端免二次查询）。
3. Android 补齐授权确认链路（此前 safe/askAll 下发危险消息误报 Send failed）：model（CommandPhase.PendingApproval + PendingApprovalInfo 等 4 个模型）、DesktopApiClient（submit 解析 approval、poll 404→终态、decideApproval 端点）、DesktopRepository（pending 状态机 + decide/clear）、ConversationDetailScreen ApprovalDialog（对齐 iOS ApprovalRequestView 三按钮）、strings 双语 +6 key、转录 uid 优先服务端 id。
4. iOS CommandReceiver.poll 404 终态特判 + 双语新 key（静态改动，本机无法编译 iOS）。
5. macOS ConversationStore.swift 一处合并行格式修复（上轮编辑事故）。

**验证**：cargo check 干净；cargo test 170 通过 0 失败（含全部新语义）。Android 编译由用户手动执行（gradle-wrapper.jar 已从 Gitee 补齐 + ~/.gradle/journal-1.lock 陈旧锁已清）。macOS 端本机 Swift 工具链编不了 Darwin → 只能静态核查（全部构造点已核对）。

**环境结论（已入 MEMORY.md）**：Windows Swift 工具链编不了 macOS 目标；Git Bash coreutils 缺失/PowerShell 无回显 → Bash+落盘+Read 模式。

## Android 无法获取 Windows 对话记录：系统排查 + 401 语义修复（已修）

**排查结论**：全链路逐环验证，Windows 服务端与网络层全部正常，根因锁定在手机端 token 失效 → 401。

**已验证正常的环节（证据）**
1. 服务端：`brewping-desktop.exe`(PID 26724) 监听 `0.0.0.0:8787`；防火墙多条 BrewPing Allow Inbound。
2. 局域网路径：用 WLAN 真实 IP `192.168.3.93:8787` + 现行 token 请求 `/api/conversations?includeArchived=1` → 200（2 条对话）——绑定/防火墙均放行（本机自测局域网 IP 的局限：不能完全等同手机跨设备访问，但 status 在线已证明手机可达）。
3. 契约：错误 token → 401 `{"error":"invalid token","success":false}`（JSON 体，客户端解析不炸）。
4. Android 代码：BrewPingApp 装配（单例 apiClient 持有 pairingStore）、`signed()` 注入、缓存/降级逻辑全部正确。

**根因**：桌面端 `~/.brewping/pairing.json` 的 token `createdAt=2026-09-11T11:48:25Z`（重新签发过）。Windows 端 token 是单例（文件丢失即重新生成），旧 token 全部失效（`authorize` 严格单值比对）。手机端 token 存 SharedPreferences（卸载才丢），感知不到桌面端重置 → 受保护接口全 401。
**帮凶**：`GET /api/status` 免鉴权 → 手机显示"在线"掩盖 token 失效；Android 把 401 归入笼统 `error` 分支只显示 "Server error 401"，用户看不出要重新配对（iOS 早已单独处理 401 → 清列表交给未配对 UI）。

**修复（Android 补齐 iOS 401 语义，6 处）**
1. `model/Conversation.kt`：`ConversationsResult`/`ConversationResult` 加 `unauthorized` 字段。
2. `api/DesktopApiClient.kt`：`fetchConversations`/`fetchConversation`/`parseConversationMutation` 401 特判 → `unauthorized=true`。
3. `store/ConversationStore.kt`：新增 `_unauthorized` StateFlow；refresh/open/createConversation 的 unauthorized 分支（清列表+loadedKey=null，对齐 iOS）。
4. `ui/ConversationListScreen.kt`：新增 `onRePair` 参数 + `RePairNotice` 卡片（Warning 图标+说明+重新配对按钮），unauthorized 优先于 unsupported/空态。
5. `ui/HomeScreen.kt`：接线 `onRePair` → `showPairFor = 当前 active ManagedDevice`（弹既有配对窗）。
6. `strings.xml` en/zh：+3 key（pairing_required/pairing_required_hint/re_pair）。注意 strings.xml 撇号转义坑：`\\'` 写错会显示反斜杠，干脆用 "has not" 措辞规避。

**用户侧验证步骤**：① Windows 托盘 Show Pairing Code → ② Android 设备卡"配对"输入 6 位码 → ③ 配对成功自动 connectToDevice → 对话列表恢复。若列表仍空，下拉强制刷新（force=true 绕缓存）。

**编译验证**：命令行 gradle 默认 JAVA_HOME 是 JDK 25.0.4.1，Gradle 8.14 不支持 → 直接跑报错（错误信息只有版本号 25.0.4.1）。用 -Dorg.gradle.java.home=D:/study/java/devlop/jdk17 指定 JDK 17.0.20.1 后 compileDebugKotlin 通过（EXIT=0）。AS 内编译用 JBR 25.0.2 能过，说明 AS 配了别的 Gradle JDK。

## Android 扫码配对：幽灵 Mac + 重复设备 + 配对码一次性机制（已修）

**用户疑问的机制确认**：配对码确实一次一用（pairing_store.rs exchange() 成功即作废）。iOS 扫码已消费那个码；**删除手机端设备不会让桌面端的码复活**。必须 Windows 端重新生成新码。桌面端配对窗在码被消费后 UI 不自动刷新（仍显示旧二维码），需关掉重开或点刷新。token 是长期单例——多台手机各自配对可并存，都拿同一个 token。

**幽灵 Mac 根因（3 个 Android bug，iOS 均无）**：
1. PairPayload 漏解析二维码 osType 字段（桌面端固定写 osType=windows，iOS PairingURLHandler 有处理）。
2. HomeScreen 扫码 pair 路径新建设备漏传 osType → 默认 Mac（ManagedDevice.new 默认值）。
3. DeviceFormDialog 扫码预填不预填 osType。
外加：扫码新建与已有设备去重失败（按 host+port 比对缺失）→ 同一台机器两条（NSD/表单一条 + 扫码一条），token 绑在扫码条（id 改为桌面 deviceId），用户激活无 token 的条目 → 401 → 不同步。

**修复（4 处，compileDebugKotlin EXIT=0）**：
1. PairPayload 加 osType 字段 + fromRaw 解析。
2. 扫码 pair 路径：新建前按 host+port 去重复用（并同步 name/osType），新建时带 osType。
3. 表单扫码预填 osType。
4. pairWithCode 识别桌面端 "invalid or expired" → 友好文案 pairing_code_exhausted（en/zh）：一次一码、去桌面端重新生成。

**验证步骤**：Windows 设置→配对→刷新生成新码 → Android 删掉重复条目 → 扫新码 → 应显示 Win 且配对成功 → 对话同步。

## 删除设备后被自动重新注册成 Mac（已修）

**现象**：删除设备 → 重新进入 App → 自动出现一台 Mac 设备，且对话仍不同步。

**根因（不是缓存）**：DeviceStore.migrateIfNeeded() —— 旧版单设备时代遗留的迁移兜底。每次冷启动且设备列表为空时，从旧 brewping_prefs（brewping.macAddress/brewping.port）读出旧配置自动 addDevice，osType 硬编码 Mac。旧配置键永不清理 → 删光设备后每次启动都复活一台 Mac；该条 host 是旧值 → 连不上 → 对话自然不同步。这是**业务逻辑缺陷**：删除本身持久化正确（removeDevice→save 同步），是另一条启动路径在重建。

**修复**：迁移成功后立即清除旧配置键（KEY_OLD_MAC/KEY_OLD_PORT）；已有设备时也顺手清理。编译通过（EXIT=0）。

**方法论（区分缓存 vs 逻辑）**：卸载重装后若设备仍出现 → 排除运行时缓存；本例通过全量 grep addDevice 调用点定位到唯一非用户触发的调用。

## 配对成功后仍循环提示"需要重新配对"（已修，本日最关键 bug）

**现象**：配对显示成功，但对话列表持续 401 → "需要重新配对"卡片循环出现。

**根因（token 键与设备 id 错位）**：pairWithCode 把设备 id 改成桌面端身份（result.deviceId）时用了 deviceStore.updateDevice(renamed)——而 updateDevice 按**条目自身 id** 查找，renamed 的 id 已变永远匹配不到 → **静默 return**；setActive(新id) 同样因 none{it.id==新id} 静默失败。后果：token 存键 BrewPing.Token.<桌面deviceId>（一直正确），但设备条目 id 永远是旧 UUID → 请求按 UUID 查 token → null → 不带 Bearer → 401 → unauthorized 卡片 → 再配对 → 同样错位 → 死循环。iOS 的改名实现不同所以正常。

**修复**：DeviceStore.renameDevice(oldId, renamed)（按旧 id 定位替换 + activeDeviceID 同步迁移）；pairWithCode 改用它。补 3 个回归测试（TC-DS-14 rename 行为 / TC-DS-15 未知 id 无副作用 / TC-DS-16 迁移后删光设备不再复活），testDebugUnitTest BUILD SUCCESSFUL。

**提示卡片触发条件**：/api/conversations 返回 401 → ConversationsResult.unauthorized → ConversationStore._unauthorized=true → ConversationListScreen 显示 RePairNotice（上一轮加的）——机制本身正确，这次修的是它不该被触发的根因。

## 空态引导优化：发现附近设备时隐藏"添加第一台电脑"（已实现）

用户确认对话同步已通。新需求：附近能发现设备时不显示 EmptyStateCard 引导；查不到（桌面端关闭）才显示。

实现：HomeScreen devices.isEmpty() 分支加 if (discovered.isEmpty()) 判断——发现列表非空时只渲染 DiscoveredDevicesSection（它自身在无新设备时也不渲染），引导卡仅在无发现时出现。编译通过（EXIT=0）。

## 配对窗扫码交互优化（已实现）

**根因**：PairDialog 是 AlertDialog（独立窗口层），悬浮于普通 composable（QrScanScreen 相机界面，Activity 内容层）之上——点扫码不关窗，相机界面被压在下面，必须手动关窗。

**修复**：点扫码立即关 PairDialog + pairScanReturn 暂存目标设备；扫到码 → 按 payload 复用/新建设备 → 重开 PairDialog（initialCode=6 位码）→ LaunchedEffect 自动配对 → 成功自动关窗，全程无手动操作；取消扫码 → 恢复配对窗（清残留旧码防误重试）。编译通过（EXIT=0）。

### 补 20：WatchSessionManager 第三次被缓冲覆盖（部分回滚，2026-09-13 18:01）

- 用户问"代码还在吗"——排查发现 `activate()` 加固（17:48）**又被盖回旧版**，但同文件里的震动/去重（16:42）还在 → 说明 Xcode 缓冲快照停在两者之间，用户构建/保存时写回。
- 已第三次重放 activate 补丁 + Read 回读验证。
- **本轮结论**：只要 Xcode 还开着 `WatchSessionManager.swift` 的旧标签页，这个问题就会重复。用户必须在 Xcode 里关闭/重载该文件后构建。其它文件已在上轮全量核对完好。

### 补 21：修复后的增量已提交推送（1991203，2026-09-13 18:06）

- merge 卷掉改动事故的收尾：6 文件增量（棕底主题 / 激活自愈 / 截断兜底 / 最近一次对话 / 标题翻转 / 记忆日志）已提交并推送 `origin/main`（aa271b9..1991203）。
- 更早的修改（HelpView、iOS 空态/去重、单麦克风、消息链路等）已在 aa271b9/cb572a6 里，无需重复提交。
- 工作区干净。**此后每次批量修改完成后建议立即 commit**，避免未提交修改在 git 操作中再次丢失。

### 补 22：麦克风按钮去掉外层奶白卡（0ec637e，2026-09-13 18:19）

- 用户指定：对话页底部只留居中麦克风按钮，**去掉外层奶白卡片底**。
- 按钮直接悬浮棕底：圆底改奶白 18%（idle）/ 抹茶绿 35%（completed），图标/转圈/✓ 全部改奶白系；外描边去掉。
- 失败提示改提亮红铺底、输入框文字改奶白（原本在奶白卡内的深咖配色全部随卡片一起移除）。
- 已提交推送 0ec637e。

### 补 23：修复点麦克风弹不出键盘（2026-09-13 18:24）

- 根因：单麦克风版把输入框渲染条件写成 `if inputFocused || hasDraft`，而 `inputFocused = true` 只能作用于已渲染的视图 → 鸡生蛋死锁，聚焦永远落空。
- 修法：新增 `@State showInput` 控制渲染（按钮点击 → showInput=true → 输入框 onAppear 里 `inputFocused = true` 聚焦弹键盘）；发送后 showInput=false 收起。
- 教训：**FocusState 的目标视图必须在聚焦前已在视图树里**；"渲染条件依赖 focused 本身"是经典死锁。
- 已提交推送。

### 补 24：麦克风点击无反应——透明输入框方案定稿（2026-09-13 18:29）

- 根因确认：程序化聚焦（FocusState 赋值 true）在 watchOS 上**唤不起键盘**（旧版点输入框能用是系统原生点击聚焦）。
- 定稿方案：**透明 TextField 铺满按钮区域做交互层**（空闲时 opacity 0.02 保留命中），麦克风/转圈/✓ 是 `allowsHitTesting(false)` 的视觉层 —— 点的是麦克风的样子、命中输入框，与旧版同一条原生路径。
- 聚焦后输入框显形（居中、1…3 行、奶白文字）；失焦且有草稿自动发送（onChange(inputFocused)）；执行中 `.disabled(inFlight)` 锁定。
- 教训：watchOS 上需要"点按钮弹键盘"时，**别用程序化聚焦，用透明输入框铺在按钮下**。

### 补 25：透明输入框重写残留重复声明（2909a78，2026-09-13 18:33）

- 上一轮整段重写 MARK 之后的内容时，文件前部（body 之前）的 `hasDraft` / `isCompletedState` / `inFlight` 三个旧属性没删，与新尾部重复声明 → "Invalid redeclaration"。
- 已删前部旧块（保留 MARK 后那份），每个属性 grep 计数 = 1，提交推送。
- **教训**：整段替换式的重写，重放前必须先 grep 目标区域里是否还残留旧实现（尤其辅助属性/助手函数在文件前部的情况）；重写完跑 `swiftc -parse` 只能抓语法，抓不住 redeclaration 这种**同一文件内**的语义冲突……实际 redeclaration parse 也能报？不——它报的是语义错误，`-parse` 模式不查。所以 `-parse` 通过 ≠ 编译通过，语义检查必须真编译。

## Android：设备离线提示 "Server error 0" → "Can't reach 设备名"（2026-09-13 18:52）

- **根因**：`executeRaw` 的 OkHttp 调用失败（IOException）返回 `RawResponse(code=0, body=null)`——code=0 是**网络层失败**（桌面端离线/超时），不是服务端错误。之前各方法把"非 200"统一归为 `Server error ${code}` / `HTTP ${code}` → 离线时显示误导性的 "Server error 0"（用户报：点进对话加载后显示 server error）。
- **修复（DesktopApiClient.kt 四处 + DesktopRepository.kt 一处）**：
  1. `fetchConversations` / `fetchConversation`：`raw.code == 0 -> null`（离线加载列表/详情 → Store 显示 cant_reach "Can't reach 设备名"）；
  2. `parseConversationMutation`：**签名改为返回 `ConversationResult?`** + `raw.code == 0 -> null`；调用方 createConversation / patchConversation 本就是 nullable 透传，无需改动；ConversationStore 的 createConversation/patch 早已备好 `result == null → cant_reach` 分支；
  3. `fetchAgentModels`：`raw.code == 0 -> null`（ModelStore.refresh 已有 null → cant_load_models 分支，离线时模型选择显示"无法加载模型"）；
  4. `decideApproval`：`raw.code == 0` 直接返回 `ApprovalDecisionResponse(success=false, error="Can't reach ${device.name}")`（与 pairWithCode 同风格；原会显示 "HTTP 0"）；
  5. `DesktopRepository.submitMessage`：`response == null`（网络失败）时 error 改 "Can't reach ${device.name}"，替代笼统的 "Send failed"。
- 不动：`pollCommandStatus` 的 null=继续轮询语义（离线轮询靠超时收敛）；`pairWithCode` / `fetchFolderRoots` / `browseFolder` 原本就正确。Android 无删除对话功能，无 deleteConversation 调用方。
- **约定（已进 MEMORY.md）**：API 层必须单独处理 `code == 0`——fetch 系返回 null 让调用方走 cant_reach / cant_load_models；写操作系直接给 "Can't reach 设备名"。禁止 "Server error 0"/"HTTP 0" 漏到 UI。
- 验证：`./gradlew :app:compileDebugKotlin -Dorg.gradle.java.home=D:/study/java/devlop/jdk17` BUILD SUCCESSFUL（二次运行 UP-TO-DATE 证明修改已被首次编译消费）。装机实测项：桌面端离线 → 点进对话应显示 "Can't reach 设备名"；发消息 / 授权决定 / 模型切换同风格可读提示。
- 环境补充：本轮 Bash coreutils 缺失（tail/dirname command not found）→ 回退 PowerShell 跑 gradle，`2>&1 | Out-File -Encoding utf8` 落盘后 Read；PowerShell 5.1 的 `*>`/`>` 默认 UTF-16，Read 会报 "binary file"。

### 补 26：修复 UI 显示 "Can't reach %1$s" 字面量（2026-09-13 19:07）

- 用户报：界面上出现 "Can't reach %1$s"（占位符没被替换）。
- **根因**：`BrewPingApp.onCreate` 构造 `ConversationStore(apiClient)` 漏传 `appContext`（默认 null）→ `msg()` 走 fallback 分支直接返回**格式串字面量** `"Can't reach %1$s"`。资源与调用参数都没问题，问题在 fallback 分支不做 String.format。
- **修复（双保险）**：
  1. `BrewPingApp.kt`：`ConversationStore(apiClient, this)` 补传 context（根因）；
  2. `ConversationStore.msg()` / `HomeViewModel.msg()` fallback 分支改 `String.format(fallback, *args)`（兜底——以后任何调用点漏 context 也不会把 `%1$s` 字面量漏到 UI）。
- **教训**：Android `getString(resId)` 不带参数时**不执行 String.format**，原样返回格式串；凡是"fallback 是格式串"的 helper，fallback 分支必须 format。带默认参数 `context? = null` 的类，新加构造调用点容易漏传——review 时重点看。
- 验证：compileDebugKotlin + testDebugUnitTest 全绿。


## Windows 桌面端：设置弹窗新增「模型配置」（内置 cc-switch Phase 1，已完成）

参考 `D:/study/ccswitch/cc-switch` 源码与迁移指南（§5.5 Phase 1：透传+多厂商+切换，不做协议转换/故障转移/熔断/CLI 接管），把外部 cc-switch 的模型转发路由能力内置到 Windows 桌面端。

**架构**
- `src-tauri/src/services/model_provider_store.rs`：`ModelProviderConfig`（id/name/baseUrl/apiKey/apiFormat[anthropic|openai_chat|openai_responses]/authStyle[auto|bearer|x-api-key]/isFullUrl/model/notes/createdAtMs/sortIndex，serde camelCase）+ `Stored{providers,currentId,proxyEnabled,proxyPort}` 落盘 `~/.brewping/model-providers.json`；首条配置自动成为 current。
- `src-tauri/src/services/model_proxy.rs`：axum 代理，默认 **127.0.0.1:15721**（对齐 cc-switch 默认端口，迁移零配置）。核心行为：按**当前选中**配置转发（每请求读 current，切换即时生效）；**鉴权替换**——丢弃 CLI 送来的 key，注入配置真实 key（Auto 按协议：anthropic→x-api-key+anthropic-version: 2023-06-01，openai→Bearer）；剥别名前缀 /claude /codex /gemini；isFullUrl 不拼路径；剥 hop-by-hop 头；**无总超时**（流式长响应，仅 connect 15s）；reqwest 不开 gzip/brotli feature 保压缩透传；SSE 用 bytes_stream 透传。
- `lib.rs`：DesktopCore 加 store+manager，5 个命令（get/save/delete/switch_model_provider + set_model_proxy），setup 自启动代理（失败仅 warn，last_error 保留原因）。
- 前端：`model-config-card.tsx`（自包含数据源+返回新快照直接 setInfo；代理启停/端口编辑/状态 Badge；列表当前✓+两步删除确认；编辑表单）；`App.tsx` SettingsSectionId 加 "models"、NAV_ITEMS 插 environment 与 pairing 之间（Layers 图标）、section 渲染接线。
- i18n：locales.ts zh/en 各 +38 个 mp.* key；`DesktopStrings.swift`（macOS）用 python 脚本机械同步（LKey 38 case + zh/en 字典各 38 条，逐 key 校验 PASS）。

**坑**
- 🚨 python 批量插 Swift 字典行时，锚点 search 在**首次插入后再次 search 仍命中同一行**（zh 字典的 envAlreadyInstalled 还在原处）→ 76 条全挤进 zh 字典。修法：逐行扫描、按锚点出现次序分别插入（第 1 处→zh，第 2 处→en）；或插入后用 finditer 重新定位时用位置偏移而不是从头 search。
- 校验 dict 值时别把 zh/en 76 条合一个 dict（同名 key 被覆盖）——按前/后半段分别比对。
- 🚨 **同一文件的多个 Edit 绝不能并行发**：并行写互相覆盖（lost update），4 处接线只有 1 处存活，tsc 以 TS6133 才暴露。同文件多处修改必须逐个顺序执行。

**验证**：cargo test 180 passed / 0 failed / 1 ignored；DesktopStrings 逐 key PASS；前端 npm run build（tsc + vite）EXIT=0 全绿。

## Windows 桌面端：cc-switch Phase 2 深度迁移 + 方案文档 S1/S2/S5/S6（已完成）

按用户要求「结合 cc-switch 源码迁移」+ 用户补充的《BrewPing模型自配置与Agent绑定-实施方案.md》完成。

**Phase 2 核心（Rust）**
- `model_transform.rs`（新，~1300 行）：Anthropic⇄OpenAI Chat 全链路（system 合并+billing 剥除、tool_use/tool_result/thinking/reasoning_content、o-series max_completion_tokens、resolve_reasoning_effort、clean_schema、usage 三桶扣减）+ **SSE 状态机**（块自动开闭、tool_calls 按 index 路由+迟到 start 兜底、UTF-8 跨 chunk、错误→error 不补终止）。TC-MT-01~08。
- `model_proxy.rs` 重写：failover 循环（MAX_ATTEMPTS=3）+ 三态熔断（3 败开 30s）+ 成功切 current + EventSink 发 `model-provider-switched`；不可重试码 400/405/406/413/414/415/422/501。TC-MPX-01~08。
- `cli_takeover.rs`（新）：Claude Code 写 settings.json env（深合并），Codex 写 config.toml（toml_edit 保注释）+auth.json；按值精确还原。TC-CT-01~06。
- 新命令：set_model_failover / get_cli_takeover / set_cli_takeover；lib.rs setup 注入 ProxyEventSink（AppHandle emit）。

**方案文档增量（S1/S2/S5/S6）**
- S1 密钥安全：`mask_api_key`（前3后4/短码整体打码）+ `ProviderView` 出参（明文 Key 绝不回传）+ upsert 见空串/掩码=保留旧 Key + `harden_permissions`（icacls CREATE_NO_WINDOW / 0o600）。TC-MPS-08~12。
- S2 厂商目录：`provider_catalog.rs`（Anthropic/Kimi/DeepSeek/智谱/自定义，预填模板**非白名单**）+ `get_provider_catalog`。TC-PC-01~04。
- S5 前端：厂商下拉预填+consoleUrl 链接、模型 datalist、Key 掩码回填、CLI 接管区（代理未运行→禁用+提示）、i18n 键对齐文档（mp.takeover* 带 {port} 插值）。
- S6：get_agent_models 返回 `preferredStillValid`；command_runner spawn 前打 model/provider 诊断日志。

**坑（本轮踩的）**
- 🚨 `breaker_record` 用 `guards.get(id)` 条目不存在时**静默丢记录**（空 map 连记 3 次失败熔断永不打开）→ 必须 `entry().or_insert_with()`。测试 TC-MPX-08 抓到。
- Rust 2021 无 let-chains（`&& let Some(x) = …` 直接编译错）；toml_edit `DocumentMut` 无 `pointer()`、serde_json::Map 也无 `pointer()`（Value 才有）→ 用 get/as_table 链。
- `vec![Ok(..)]` 推断不出 error 类型 → 测试里 `type Chunk = Result<Bytes, io::Error>; let chunks: Vec<Chunk> = …`。
- 本机 WorkBuddy 宿主 env block 有重复键（https_proxy/PATH/HTTPS_PROXY）→ `swift build` 崩 NativeDictionary，PowerShell env provider / cmd / ProcessStartInfo 三条绕过全被沙箱/环境堵死 → macOS 端 DesktopStrings.swift 本轮未跑 swift build（纯数据行改动，留待干净终端）。
- tauri dev 报 "Port 1420 already in use"：残留 vite 进程 → `Get-NetTCPConnection -LocalPort 1420` 找 OwningProcess 杀掉重启。

**验证**：cargo test **206 passed / 0 failed**（+26）；npm run build EXIT=0；tauri dev 实机重启。

## Windows 桌面端：CLI 接入两 bug（列表不显示 + loading 永转）+ Agent 模型区块（已完成，21:46-22:30）

**用户报**：① 本机已装的 CLI（OpenCode/Claude Code/Codex）不在接入列表、不可选；② 接入按钮点不动、配置列表 spinner 永转。另追加需求：模型配置页要看每个 Agent 配的模型列表。

**根因（两个 bug 两条根因链）**
1. **loading 永转 / info·cliInfo 全 null / 按钮禁用 = tokio Mutex 不可重入死锁**：`model_proxy::apply()` 持 `runtime` 锁期间调 `self.status().await`，status() 再请求同一把锁 → 永久死锁。setup spawn 的自启动 apply 卡死持锁后，所有调 `model_proxy.status()` 的命令（get_model_providers 等）永远 pending → 前端 Promise.all 不 settle。**修**：apply 主体包 `{}` 块，锁在块尾释放后再调 status()。
2. **CLI 列表不显示 = 旧 DTO 硬编码两字段**：`CliTakeoverInfo { claude_code, codex }` + `config_file.exists()` 判断"已安装"（装了 CLI 但没生成配置 → 不显示）。**修**：重构为动态列表 `items: Vec<CliTakeoverItem{id,name,installed,supported,active,config_file,exists}>`，`ALL_KINDS` 遍历；installed 用 `which::which(binary_name)`（对齐设置页语义）。扩展新 CLI 三步：加枚举值 → 补 binary_name/display_name/config_file 分支 → 补 enable/disable 实现并改 `is_takeover_supported`，前端**零改动**。

**接入按钮交互变更**：去掉"代理未运行禁用接入"，`set_cli_takeover` enable 时若代理未跑自动 `set_proxy(true,port)+apply(true,port)`（否则 CLI 指到 127.0.0.1:port 无人监听）。

**前端防御**：refresh 改 `Promise.allSettled`（两个请求独立 catch，loading必定结束）；CLI 区渲染 `cliInfo.items` 动态列表（未安装/暂不支持徽章+按钮置灰）。

**新需求「Agent 模型」区块**（model-config-card 底部）：`getAgents()` 拉列表 + 逐个 `getAgentModels()`（本机文件读取，量小）→ 摘要行显示当前生效模型（`preferredModelId ?? activeModelId`，"默认"/"跟随 CLI"徽章 + `preferredStillValid=false` 警示）；展开按 provider 分组渲染模型 chips，点击 = `setDefaultModel(agentId, model, provider)`，另有"清除默认"回落 CLI 配置。新 i18n 键 10 个（mp.cliNotInstalled/takeoverUnsupported/takeoverEmpty/agentModels×7），DesktopStrings.swift 三处同步（键集 diff 校验 0 差异）。

**坑（本轮）**
- 🚨 **WorkBuddy 宿主会话里 PowerShell/bash 回显通道会坏**（命令能跑但 stdout 拿不到/exit 1）→ 统一用 `Out-File/Set-Content 落盘 + Read 工具读` 的模式；Bash 沙箱 PATH 全坏（dirname/tail 都没有），一切走 PowerShell。
- 测试轮询竞态：`wait_for_reply` 原来只等"assistant/error 条目出现"就返回，但收尾是 `append→await→set_latest_command(None)` 两步，高负载下轮询撞进间隙 → TC-CR-02 偶发假阳性（`latest_command_id=Some`）。**修**：等待条件加 `latest_command_id.is_none()`（指针永不清则超时 panic，不变量仍被强制）。三条收尾路径（fail_command/exit0/非零）本身都会清指针，产品代码无 bug。
- 测试辅助 `fn item(info: &Info, id: &str) -> &Item` 两个输入引用 → 必须 `<'a>` 显式标生命周期（E0106）。

**验证**：cargo test **208 passed / 0 failed**（TC-CT-07 动态列表 + TC-CT-08 不支持拦截新增）；npm run build EXIT=0；tauri dev 实机重启（先清 1420 残留 vite）。

## Lody「新建 Provider」迁移方案（只读取证）

产出 `docs/BrewPing-Provider管理-Lody新建Provider迁移方案.md`。Lody 侧核心结论（`agent-config-dialog.tsx` ~2600 行 + `atoms/agents.ts` + `machine-flock.ts` + `lody.ts` 逐文件取证）：Provider = `AgentConfigMeta` 配置行（machine flock 文档 `['agentConfig', id]` 行族，按机器隔离，flock 权威 + legacy repo meta 两代合并）；四类选项 builtin/preset/custom/registry；**preset 本质是 env 注入模板**（DeepSeek/GLM/MiniMax/MiMo over Claude Code，`buildPresetEnv` 合并 fixedEnv+token，credentialModes 按 token 前缀 sk-/tp- 自动切换）；创建 builtin 有 live probe 门禁（缓存放行渲染但不放行创建），custom 需 `testedCustomKey === customAcpKey` 精确匹配；删除 managed builtin 先写 `builtinAgentOptOut` 再删行（否则启动自动注册会回添）；providerSetup 是 durable intent 行（探测成功才发布 config，取消用收敛标记）；红线：凭据只在 config.env、launch 字段 spawn 前冻结、binary 状态绑定 agentType 防串台。

BrewPing 映射：新增 `provider_store.rs`（providers.json + **ignored 即 optOut**）+ `provider_presets.rs`（预设表值直接照搬）+ 4 端点（GET/POST/DELETE `/api/providers`、POST `/test`=discover_one 版本探测）+ 两处 spawn 加 `.envs()`（与 current_dir 同点）；不迁 CRDT/registry 下载/providerSetup/ACP 认证。验收含 token 明文脱敏（TC-SEC-01，日志+转录）。未改源码。

## Windows 桌面端：厂商预设一期落地（S1+S3+S5，参考《cc-switch预设供应商能力-迁移至BrewPingWin方案.md》，23:00 前后）

按方案文档一期拍板范围实施：**S1 后端目录扩充 + S3 回填增强 + S5 高级选项（拉模型列表/映射说明/端点预览）**；S2 卡片网格按 Q5 结论跳过（一期仅 6 条，下拉够用）。

**后端（Rust）**
- `provider_catalog.rs` 重写：`CatalogEntry` +4 字段（`display_name`/`category`/`website_url`/`models_url`）+ 新增 `CatalogCategory` 枚举（official/cn_official/aggregator/third_party/custom，serde snake_case）+ `find(id)`；CATALOG 换成一期 6 条：**kimi/deepseek/zhipu/xiaomi/minimax 全走 Anthropic 端点（零协议转换）+ custom**（anthropic 官方按拍板移出一期）；deepseek/zhipu 从 openai_chat 修正为 anthropic 端点（`api.deepseek.com/anthropic`、`open.bigmodel.cn/api/anthropic`）。单测 4→11（TC-PC-05~11：console_url https、单条 custom、**禁推广参数 aff=/invite**、display_name 非空、一期全 anthropic 护栏、一期 id 齐全、models_url https-or-empty）。
- `services/model_list.rs`（新）：`parse_openai_models`（`{"data":[{"id":…}]}` 解析，异常输入空 Vec 不 panic）+ `dedup_sort`；TC-ML-01~03。目录层保持零 IO，网络放 command 层。
- `model_proxy.rs`：`http_client()` 私有 → `pub(crate)`（列模型复用连接池，1 行）。
- `lib.rs`：新命令 `fetch_provider_models(provider_id, api_key: Option<String>)`——Key 解析顺序：前端明文（不含 •）→ 已存配置里**同 base_url** 的真实 Key（route_chain）→ 都没有报 missing api key；401/403 → "invalid api key"；非 2xx/解析空 → 报错让前端回落静态候选。**明文 Key 不进日志/错误信息**。注册进 generate_handler。

**前端（React/TS）**
- `types.ts`：`CatalogEntry` +4 字段 + `CatalogCategory` 联合类型；`tauri.ts`：`fetchProviderModels(providerId, apiKey|null)`。
- `model-config-card.tsx`：① `applyVendor` 增强——**model 随厂商回填**（`c.models[0]`，已填保留；🔴 代理映射模式下 cfg.model 覆盖请求体，残留旧型号必 400）+ **isFullUrl 强制重置 false** + name 用 displayName 回填 + 切厂商作废已拉取列表；② 厂商下拉改 **optgroup 按 category 分组**（custom 恒最后）+ 显示中文名；③ 新增「高级选项」折叠区：获取模型列表按钮（modelsUrl 为空禁用+提示）→ 可点选模型 chips（fetched ?? 静态候选）、模型映射说明文案、生效端点预览（前端镜像 openai_chat_endpoint 智能补 /v1 逻辑）；④ `closeForm()` 统一复位临时状态。

**i18n**：mp.* 新增 15 键（advanced/availableModels/fetchModels/fetching/fetchFailed/fetchUnsupported/fetchNeedKey/invalidKey/modelMappingHint/effectiveEndpoint/presetCategory.×5），locales.ts zh/en + DesktopStrings.swift 三处同步。校验：locales 每个 mp 键恰好 2 次、Swift 字典 mp 条目 = 2×cases。
**既有差异（非本轮引入）**：`mp.auth.x-api-key` 在 locales.ts 有、DesktopStrings.swift 无对应 case（macOS AUTH 选择器无此项）——历史遗留，未动。

**验证**：cargo test **218 passed / 0 failed**（208-4+11+3=218）；npm run build（tsc+vite）EXIT=0；tauri dev 重启实机加载新代码。五家真实 Key 实连验收（方案 §6.2 第 13 条）需用户手填 Key 后验证。

## 跨端厂商配置写入：Claude Code / Codex / pi（对标 cc-switch，01:00-02:30）

**用户需求**：「让 Codex、Claude Code 和 pi 都能以相同方式写入配置。请先查看 cc-switch 的源码以及我提供的相关文档，确认当前配置写入逻辑是否为通用实现；如果不是，请说明各工具之间的差异，并帮我实现针对各个 agent 的方案。」

**调研结论（cc-switch 不是通用实现）**
- `services/provider/live.rs::write_live_snapshot(app_type, provider)`（L1277）是 `match app_type` **分派器**，每个 CLI 一个专属模块：
  - `Claude` → `sanitize_claude_settings_for_live()` 后 `write_json_file(get_claude_settings_path(), …)` = **整体覆盖** `~/.claude/settings.json`（剥离 `api_format`/`apiFormat`/`openrouter_compat_mode`/`openrouterCompatMode`，见 L168-178）。
  - `Codex` → `write_codex_provider_live_with_catalog()`：双文件（`~/.codex/config.toml` + `~/.codex/auth.json`），OAuth 体系极重（`experimental_bearer_token` / 顶层 `openai_base_url` / 保留 id `openai`·`ollama`·`lmstudio`）。
  - `Pi` → `Err(InvalidInput("Pi providers use the Pi provider service"))`，走 `services/provider/pi.rs` 专属服务（增删改 + key 不可改名 + DB 双写 rollback）。
  - `OpenCode`/`OpenClaw`/`Hermes` → 增量模式（`set_typed_provider` 深合并 provider 段）。
- 结论：**没有可复用的通用写入函数**，三者形态差异极大（单文件覆盖 / 双文件 TOML+JSON / 增量 JSON + 默认项成对）。

**实现（路线 A：新增独立 `*_config.rs`，与既有 `cli_takeover` 并存不冲突）**
- `services/claude_config.rs`：整体覆盖 `~/.claude/settings.json` 的 `env` 段（顶层其余键保留）—— `ANTHROPIC_BASE_URL`+`ANTHROPIC_AUTH_TOKEN` + 三档 `ANTHROPIC_DEFAULT_{SONNET,OPUS,HAIKU}_MODEL`(+`_NAME`，留空不写)。写前 sanitize。删除只摘 `ANTHROPIC_*`（env 空则摘整段）。12 测试。
- `services/codex_provider_config.rs`：toml_edit 写 `~/.codex/config.toml`（**保注释**）—— 顶层 `model_provider` + `[model_providers.<key>]` 的 `name`/`base_url`/`wire_api`(chat|responses) + provider 作用域 `experimental_bearer_token`；保留 id 大小写精确拦截；`model` 留空不覆盖顶层。**绝不碰 auth.json**（TC-CX-03 逐字节断言）。17 测试。
- `services/pi_config.rs`：增量写 `~/.pi/agent/models.json` 的 `providers.<key>`（`baseUrl`/`apiKey`/`api`/`models[]`）+ `settings.json` 的 `defaultProvider`+`defaultModel`（成对；settings 路径从 models 同目录派生）；key 不可改名；activate 取模型数组首个；原子写 temp+rename。**绝不碰 auth.json**（TC-PI-03）。19 测试。
- 命令层 `lib.rs` +10 条（get/save/delete_claude_provider；get/save/delete/activate_codex_provider；get/save/delete/activate_pi_provider）；前端 API `types.ts`+10 类型、`tauri.ts`+10 封装。
- 前端组件 5 个：`claude-provider-form.tsx`（单例编辑 + 三档 + otherKeys 只读展示）、`codex-provider-form.tsx`/`codex-provider-panel.tsx`（列表 + 生效切换）、`pi-provider-form.tsx`/`pi-provider-panel.tsx`（列表 + 默认项）。接入 `model-config-card.tsx` 对应 tab（`claude-code`/`codex`/`pi`）—— 各自独立状态块 + refresh + 保存/删除/activate handler，纯在 tab 条件下渲染。i18n 新增 `cl.*`/`cx.*`/`pi.*` 三组（zh/en 对齐）+ `oc.errBaseRequired`/`oc.errBaseScheme` 两个通用地址校验键。

**踩坑**
- 🚨 **Edit 事故**：想把 handler 插到 `copyEndpoint` 前面，先误把 `old_string` 写成整段函数、`new_string` 只留签名 → 函数体被删。靠后续 Grep 发现空缺才补回。**教训：插入型编辑要用「纯锚点行」，不要把整段实现当 old_string。**
- `t(tier.labelKey)` 传 `string` 报 TS2345 → `labelKey` 标 `DictKey`，并从 `i18n/index.tsx` re-export `DictKey`（原本只在 locales.ts）。
- 三个新表单引用的 `oc.errBaseRequired`/`oc.errBaseScheme` 词典里不存在（opencode 表单当时直接硬编码）→ 一并补进 zh/en。

**验证**：`cargo test --lib` **288 passed / 0 failed / 1 ignored**（+48 新测试）；`npm run build`（tsc + vite）EXIT=0（仅既有 chunk-size 警告）。临时产物 `.build-*.txt`/`.tsc-out.txt`/`.cargo-test.txt` 已清理。

**遗留**：Mac 端 Swift 与 Android 的 aider→pi 替换不在本轮范围；pi 6 份关联文档待批量修订；Claude 的 `delete` 入口（单例面板的删除按钮）已实现但未做端到端手测。

