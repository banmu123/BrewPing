# 2026-09-24

## README 重构为「正式发布版」（Windows 侧，21:4x）

用户要求：iOS 已正式上架 App Store，README 要从「开发中 / 审核阶段」切换为「已发布产品」定位。

### 先做事实核实（这一步决定了后面所有措辞）

- **App Store 链接**：全仓扫 `apps.apple.com` / `itunes.apple.com` / `play.google.com` → **0 命中**；
  向用户确认后决定：**首屏只写「Available on the App Store — search for BrewPing」，不加超链接**
  （避免链接失效/国区外区不一致）。🔑 **绝不能凭空造一个商店 URL。**
- **Android 状态**：用户确认**未变** —— 仍是「已构建、未提交 Google Play」。
- **macOS / Windows**：下载入口保持「GitHub Release `v1.0.0` + commitbrew.com 官网」。
- **支持的 Agent（读代码确认，不靠猜）**：`Sources/Agents/AgentDiscovery.swift` 的目录 +
  `AgentManager` 的工厂 + `AgentConfigDiscovery` 的分支 = **正好 4 个**
  （`opencode` 会话 PTY / `claude` 无头 / `codex` 无头 / `pi` 无头）。
  ⚠️ **`aider` 在产品代码里已经没有**（只剩 docs/ 历史文档与 Android 测试夹具里的字样），
  用户提示词里把它列为「当前支持」是**过时的**，**没有写进 README**。
- 所有 README 引用的本地资源（logo / 2 张截图 / docs / workflows）逐个确认存在。

### 重构后的结构（中英完全对齐，各 18 个一级章节）

`Download → Screenshots → What BrewPing is / is not → Who is it for → Why not SSH/远程桌面/云端
→ Features → How it works（ASCII 图）→ Platform status → Supported coding agents → Getting started
（4 步）→ CLI → Security & privacy → Architecture → Documentation → Contributing & development
→ Roadmap → Trademarks → License → Credits`

### 按 §13 清掉的过期内容

- 首屏那句 `Availability:` 引用块（含 **In App Review / TestFlight / no App Store listing yet**）→ 删，改为 Download 表 + 平台状态表
- `docs list` 里的 **[App Store pre-submission review]（原文写着「at the point the iOS build went into App Review」）→ 从 README 移除**；文件仍留在 `docs/`（历史归档，符合 §13「挪到 docs」）
- **Testing & CI 里那段「release-mac.yml 缺 Apple secrets、v1.0.0 的 Mac 包是本地手工挂的」→ 删**（属发布流程内部细节，PROJECT_STATUS 已有）
- **Requirements 表 → 并入 Platform status**；**Project Structure 长树 → 删**（CONTRIBUTING 已有）；HTTP API 的 22 行大表 → 压成 Architecture 里的一小段（否则会**丢信息**，见下）

### 我主动做的两个判断（需要用户知晓）

1. **没有删掉 HTTP API 的信息**，而是压缩进 Architecture：认证模型一行 +
   端点按前缀分组（`/api/agents…`、`/api/message…`、`/api/session…`、`/api/approvals…`、
   `/api/conversations…`、`/api/folders…`）。原因：删掉就会在仓库里**永久丢文档**，
   而任务限定「不要改其他文件」→ 不能另建 `docs/HTTP-API.md`。
2. **Roadmap 里没有写「Optional remote connectivity」**（提示词给过这个例子）：
   因为我自己写的 README / PROJECT_STATUS 明确声明过「官方公网中继**不在路线图**里」，
   写进去会自相矛盾。改成 §「Not planned」正向声明。

### 连带必须修的一处矛盾（超出 README 范围，已最小化修改）

`docs/PROJECT_STATUS.md` 仍写「iOS/watchOS **Submitted to App Review — not publicly released** /
in App Review, TestFlight testers only」，与新的 README **直接冲突**。按「确实必要」原则只改发行事实：
- §1 总状态行、§1.1 表 iPhone 行 → **Available — App Store**
- §2 平台表 iPhone / Apple Watch 备注 → **available on the App Store**
- §6 已知限制第 5 条 → 改为「**Android 与 Wear OS 未发布**」，并点明 iOS/watchOS/macOS/Windows 已发布
- §6 第 10 条、开篇 kind-3 措辞、复核日期 → 同步

⚠️ **未动 `SECURITY.md:47`**（「test against the latest `main` (or the newest TestFlight build)」）——
属措辞过时而非矛盾，按「不要改其他文件」留着，**建议后续改成「the latest App Store release」**。

### 验证

- 过期内容扫描（App Review / TestFlight / Waiting for Review / Guideline 2.1 / 审提审被拒 / coming soon
  / Pre-Submission）：**双语 0 命中**
- 章节对齐：EN 18 / ZH 18，逐条对应
- 本地链接：按各自文件所在目录解析 → **0 broken**；站内锚点 **0 未命中**
- 平台状态三处口径（README-EN / README-ZH / PROJECT_STATUS）逐项比对 → **全部一致**
- 营销化/绝对化措辞（world-class / 100% secure / 革命性…）：**0 命中**
- `git diff --stat`：`README.md`、`README.zh-CN.md`、`docs/PROJECT_STATUS.md`
  —— 3 files changed, 400 insertions(+), 551 deletions(-)（净减 151 行）

### 未提交

⚠️ 按要求**没有 commit**（用户明确「不要自行提交 git commit」）。
另：本地仍有 **1 个更早的未推送提交** `d1f8317`（记忆文件），因为沙箱里 `git credential fill`
拿不到凭据 → `could not read Username`，**不是内容问题**。

### 踩到的校验坑（值得记住）

自己写的校验脚本**连续误报**：① 相对链接 `../CONTRIBUTING.md` 按仓库根而不是文件所在目录解析 →
假报 8 个 broken；② 用固定子串比对时，中文表格用 `——`、英文用 `—` 且**跨行折断**（`has **not been\n
submitted to Google Play**`）→ 假报 2 处不一致。**结论：文档校验脚本必须先 normalize（折叠空白）并按
文件所在目录解析，否则会浪费一轮去查根本不存在的问题。**

---

## Android/iOS 功能对齐审查（第二版，21:5x）

重写了 `docs/BrewPing-Android-iOS-功能对齐审查.md`（旧版基线 `2760047`/09-19 已过时）。
新基线 `d1f8317`。方法：双端源码清单 → 20+ 能力探针 → 差异逐条回读代码坐实。

### 关键结论

**核心链路 Android 已对齐 iOS**（发现 / 配对 / 签名 / 命令+轮询 / 审批三档 / 多对话 / 置顶归档 /
工作目录 / 模型 / 多设备 / 语言三档 / Lathe 主题 / Markdown / 令牌安全存储），而且**有两项反超**：

- **会话启停 UI**：Android 有（`HomeViewModel.startSession/stopSession` 已接 UI）；**iOS 0 命中**（只在 DemoBackend 模拟）
- **设备重命名**：Android 有（`DeviceStore.renameDevice`）；**iOS 0 命中**

### 真实缺口（要补的）

1. **★ Demo 模式完全缺失**（最大件）：iOS `DemoBackend.swift` 656 行覆盖 **16 个端点** +
   `DemoURLProtocol`（URLSession 层拦截）+ 帮助页 `Try it without a computer` 段；
   Android 全仓 0 命中（`HelpScreen.kt:61` 自己都写着「尚未实现」）。
   → 建议方案：照搬 iOS 思路但用 Android 原生机制 —— **OkHttp `Interceptor`** 短路，
   新增 `:core/demo/DemoBackend.kt` + `DemoInterceptor.kt`，`ManagedDevice` 加 `isDemo`。
2. **帮助页缺 2 段**：iOS 8 段 vs Android 6 段 —— 缺 `Language`、`Try it without a computer`。
3. **手机端语音**：iOS 手机端有 20 处 `SFSpeechRecognizer`（转写手表音频）；Android `:app` **0 处**
   （只有 `:wear` 的 `RecognizerIntent`）→ 建议用 `SpeechRecognizer` 补本机口述。
4. **权限被拒→去设置**：iOS `PermissionCenter` 有串行请求 + 跳设置；Android 只在扫码页处理相机。
5. **2 处硬编码文案**：`HomeScreen.kt:330 "Add Device"` / `:357 "Edit Device"`，
   且 `strings.xml` **没有** `add_device`/`edit_device` 键（是漏用资源）→ 中文界面下显示英文。

### 更正旧结论（重要）

旧版把「Help/隐私入口」「令牌明文」列为 P0 —— **两项都已落地**（`HelpScreen.kt` 已存在；
`PairingStore.kt` 已是 Keystore AES-256/GCM + 明文透明迁移）。旧版说 Android 无手表端也已过时
（`:wear` 7 个 kt）。**唯一仍然成立的旧结论是 `CommandReceiver.kt` 死桩**（23 行空壳、生产未接线）。

### 一处三端共有待办（不是 Android 独缺）

服务端权威 `run.phase`（含 `stalled`）**只有 Wear 端在消费**：`:core` 已解析成 `CommandRunStatus`，
但 **Android 手机端 0 处消费、iOS 手机端也 0 处**（两端都用各自的客户端 `CommandPhase` 七态：
idle/sending/delivered/working/completed/completedRaw/failed，**这一层两端完全同名同态**）。

### 探针踩的坑（复用）

正则 `stalled` 会命中 **`installed`**（子串）→ 假报 iOS 有 stalled 展示；必须用 `\bstalled\b`。
同理 `updatedAtMs` 会污染 `run` 字段的检索 → 查 JSON key 要用 `"run"` 带引号精确匹配。

---

## Android 对齐 iOS 实施（22:0x 起）

用户「开始实施」→ 按对齐清单动手。

### P0：Demo 模式（移植 iOS）

**做法**：照搬 iOS 的拦截思路，但换成 Android 原生机制 —— iOS 用 `URLProtocol`，Android 用
**OkHttp `Interceptor`**。因为拦截发生在 HTTP 层，`DesktopApiClient` 里 **40 个方法一行都没改**。

| 文件 | 说明 |
|---|---|
| `:core/demo/DemoBackend.kt`（650 行） | 移植 iOS `DemoBackend` 的 **16 个端点**，响应形状与真实桌面端**逐字段同构**（含 `pending_approval`、providers→models 两层、`folders` 的 Windows 形态、409 删除语义） |
| `:core/demo/DemoInterceptor.kt`（59 行） | 只接管 `request.url.host == "demo.brewping.local"`，其余 `chain.proceed()`；带 250ms 延迟让 Sending/Working 可见 |
| `:core/demo/DemoStrings.kt` | 文案**注入**而非硬编码：`:core` 是纯逻辑库、不带资源，由 `:app` 从自己的 `strings.xml` 构造后传入 → Demo 文案同样跟随应用内语言切换 |
| `:core/api/DesktopApiClient.kt` | 新增**可选** `interceptor` 构造参数（默认 null，生产不注入；既有 `DesktopApiClient()` / `DesktopApiClient(pairingStore)` 调用点与测试全部不受影响） |
| `:core/model/ManagedDevice.kt` | `isDemo` **由主机名派生**（不新增存储字段 —— 旧 JSON 缺新键会导致反序列化失败、清空用户设备列表，iOS 同此约定） |
| `:app/store/DeviceStore.kt` | `addDemoDevice(name)` **幂等**（已有则切为当前设备） |
| `:app/ui/HomeViewModel.kt` | `isPaired` 对 Demo **直接放行**（对齐 iOS `DeviceAuth.isPaired`）；新增 `addDemoDevice` |
| `:app/BrewPingApp.kt` | 挂拦截器 + 从 `R.string.demo_*` 构造 `DemoStrings` |

### P1/P2

- Help 页补 **Language 段**（三档选择 + 对勾）与 **Try it without a computer 段**（Demo 入口），
  段序与 iOS `HelpView` 完全一致（8 段）。
- 修掉 `HomeScreen.kt` 的 `Add Device` / `Edit Device` 两处硬编码（中英各补 2 条资源）。
- 文案 key：`values` / `values-zh` 均 **138 条，集合一致**。

### 🔴 实施中测试抓出的两个真实缺陷（iOS 的 Demo 后端同样存在，未去改 iOS）

1. **归档后删不掉**：`DELETE /api/conversations/:id` 只查静态基表，而归档是经 `PATCH` 写进
   `conversationOverrides` 的 → 永远读到 `archived=false` → 一直 409。**改为查合并后的视图**。
2. **内置对话删不掉**：删除只从 `createdConversations` 移除，静态那两条会立刻复现 →
   Demo 的「归档 → 删除」是假的。**新增 `deletedConversations` 登记集合**。

另把 `/api/agents` 的 `active` 改为按当前 Agent **动态**计算（静态表里的标记切 Agent 后会过期）。
这三点都是「Demo 走不完整流程」的真实问题，不是风格问题。

### ⚠️ 更正原审计的一处误判

原审计把「手机端语音输入」列为缺口，**依据有误**。回读 iOS 后确认：
**iOS 手机端没有任何麦克风入口**，`SFSpeechRecognizer` 只用于
`WatchConnectivityManager.transcribeAndForward(audioURL:)` —— 转写**手表传来的音频**。
Android 手机端 0 处语音（与 iOS 一致），手表端用系统 `RecognizerIntent`。
→ **在手机端加麦克风是 Android 独有增强，不是对齐**，因此本轮**没有实现**。
「权限被拒→去设置」同理：手机端唯一需要的危险权限是相机（扫码页已处理），NSD 无需权限。

### 验证

- `:core:testDebugUnitTest` + `:app:testDebugUnitTest` → **114 例 0 失败**（core 67 = 原 36 + DemoBackendTest 31；app 47）
- `:app:assembleDebug` + `:wear:assembleDebug` → **BUILD SUCCESSFUL**
- 中英 key 集合一致（138/138）；硬编码用户可见文案复查 **0 命中**

### 踩坑

- **aapt2 拒绝 `\uXXXX` 转义**（报 `Invalid unicode escape sequence in string`）：本项目约定是
  **破折号直接写字面 `—`、撇号写 `\'`**，别用 `\u2014`。
- **JVM 方法名不能含 `/`**：Kotlin 反引号测试名里写 `普通 / git 徽章` 会报
  `Name contains illegal characters: /`。
- **用 `repr()` + shell 校验反斜杠不可靠**（输出里 `\\` 与 `//` 混着出现，看不错真值）→
  直接改**用 Read 工具回读原文**。

---

## 收口：删除 `CommandReceiver.kt` 死桩（唯一遗留）

### 拍板：① 删除（不选 ② 收敛）

原审计给的二选一里，**② 建立在不成立的前提上**——它假定命令生命周期「散在」四个文件。
回读后实际分层清晰且单向：传输 `:core DesktopApiClient.kt` → **状态机（唯一权威）
`:app DesktopRepository.kt`**（`_commandPhase` + `submitMessage`/`startCommandPolling`/
`decideApproval`）→ 转接 `HomeViewModel`（只透传）→ 展示 `ConversationDetailScreen`。
**生命周期早已收敛，只是不叫 `CommandReceiver`。**

iOS 之所以要 `CommandReceiver`+`CommandSubmitter` 单例，前提是 WCSession 能在 App 后台、
界面尚未创建时唤醒进程投递命令（`ios/CommandReceiver.swift:42-46` 有完整故障记录）。
**该前提在 Android 不存在**：① Wear 的 `WearRepository.submit()` → `DirectHttpTransport`
→ **直连桌面端**，不经手机；② `:app` AndroidManifest **只有 MainActivity**（LAUNCHER +
`brewping://` 深链），无 Service / BroadcastReceiver / WorkManager，不存在后台投递通道；
③ 解耦本已存在——`DesktopRepository` 就是 `BrewPingApp` 级单例。
故 ② 是「为不存在的问题引入抽象」，收益为零且会制造双头命令状态。

### 改动

- 删 `Android/app/src/main/java/com/brewping/android/CommandReceiver.kt`（23 行空壳）
- 删 `Android/app/src/test/java/com/brewping/android/CommandReceiverTest.kt`（5 例）
- `MainActivity.kt` 去掉 `CommandReceiver()` 实参；`HomeViewModel.kt` 去掉 import +
  构造参数 + Factory 参数（共 4 处）

### 顺带修正的文档漂移（两处，都不是本次引入）

1. **测试数漏同步**：上一轮 Demo 工作已把 Android 单测从 83 推到 **114**（core 36→67，+DemoBackendTest 31），
   但 README / PROJECT_STATUS / CONTRIBUTING 仍写 **83**。本次删除 5 例后为 **109**，
   四处（README.md / README.zh-CN.md / docs/PROJECT_STATUS.md / CONTRIBUTING.md）**已全部改为 109**。
2. **对齐文档自相矛盾**：§0 表格写 Demo「单测 24 例」，同文 §5 写「31 例」→ 实测 `DemoBackendTest.kt`
   为 **31** 例，已统一为 31。

### 验证（本机实测）

- `JAVA_HOME=<jdk17> ./gradlew.bat test assembleDebug :wear:assembleDebug` → **BUILD SUCCESSFUL**
- 解析 JUnit XML：`:core` **67** + `:app` **42** = **109**（debug/release 两变体一致），
  `grep -c '<failure\|<error'` = **0**（断言过，非仅凭 BUILD SUCCESSFUL）
- app 47→42 与删掉的 5 例严格吻合

### 同批「未被调用」条目的复核结论（本次未动手）

- ✅ 已自然消解：`HomeViewModel.statusPollJob`（现 `modelPollJob` 且确有赋值）、
  `Color.kt` 12 个颜色常量（现 15 个 `Latte*` **全部在用**）
- 🔍 **仍然成立、零外部引用**：`HomeViewModel` 的 `messageText` / `updateMessageText` /
  `sendMessage()` 自成死链（`_messageText` 仅自读自写 + resetState 清空；已被
  `sendInConversation` 的对话级路径取代）。⚠️ 唯一同名命中是 `WearProvisionSender` 的
  Wearable `MessageClient.sendMessage`，与此无关。**属独立的产品意图判断（复活还是删除），
  未擅自处理。**

### 踩坑（新）

- 🚨 **我对同一文件并行发了 4 个 Edit → 1 个报 `EBUSY: resource busy or locked`**。
  项目早有铁律「同一文件多编辑不得并行」，这次自己踩了：**同文件编辑必须串行**。
- 测试数别从 gradle stdout 取（它不打印），要解析 `*/build/test-results/*/TEST-*.xml`
  数 `<testcase `；并单独 grep `<failure|<error` 断言零失败。



