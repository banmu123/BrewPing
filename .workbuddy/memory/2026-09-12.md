# 2026-09-12

## 多对话管理（方案 P1-P4）全部完成

- **P4 前端换源收尾**（本日完成）：
  - 修复 `App.tsx` 三处笔误：坏的 `refreshActiveConversation` 占位函数改为 `fetchConversation(id)`（事件监听与 init 共用）；`void init;` → `void init();`；agent select 的 `cn("COMPOSER_PILL", "composer-pill")` 改用 chat-view 导出的 `COMPOSER_PILL_CLASS`。
  - **发现上会话改动部分丢失**：`api/tauri.ts` 里 6 个对话函数（listConversations 等）实际不存在、`types.ts` 缺 `DesktopStatus.activeConversationId`（Conversation 类型在）。本次补齐：tauri.ts 加 list/get/activate/setArchived/delete/togglePin 六个 invoke 封装；types.ts 加 `activeConversationId: string | null`。
  - `npm run build`（tsc + vite）通过（exit=0，vite 7.86s）。
- **桌面端重建重启**：kill 旧进程（brewping-desktop 34352 / vite 31140 / webview 21004）→ `npm run tauri dev`（WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--no-sandbox）→ `/api/status` 200 online；`/api/conversations`、`/api/agents` 均返回 401（Bearer 鉴权拦截，证明路由已注册而非 404）。
- **后端**（上会话已完成并 cargo test 148 全绿）：conversation_store / command_runner / conversation_api 三个新模块 + 6 条 HTTP 端点 + lib.rs 7 个 Tauri 命令，本会话未再改动。

## 环境坑（本会话新踩）

- **WorkBuddy PowerShell 工具 stdout 全程被吞**（连 Write-Output 都无输出），bash 也缺 coreutils（tail/sleep/dirname not found）。可靠模式：命令输出重定向到文件（PS 5.1 `>` 产 UTF-16LE，Read 工具会判为 binary）→ 用 `node -e` 读文件、去 `\x00` 后 console.log（node 的 stdout 正常）。
- **Edit 工具偶发"报成功但不落盘"**：本会话 6 处编辑有 3 处第一次没写进文件（deps 数组和 void init 写入了，其余没写）。改完必须 Grep/Read 验证，不要信任成功返回值。
- **旧 `npm run tauri dev` 后台任务会持续 watch 并自动重编**：kill 它的子进程（vite/brewping-desktop.exe）后任务才退出；它退出前 8787 一直被占。重启桌面端前先确认 8787 释放。

## Composer 底部操作区样式优化（2026-09-12 上午）

- 用户反馈（附截图）：图标/文案不对齐、原生 select 弹层丑、模型名溢出换行。
- **新建 `src/components/chat/composer-dropdown.tsx`**：自定义圆角卡片下拉（替代原生 select）——触发器 = 图标 + 单行 truncate 文案 + chevron（同行垂直居中，h-7）；弹层向上展开（`bottom-full mb-2`），`rounded-xl border bg-popover p-1.5` + 暖色阴影 + `composer-popup-in` 动画（app.css 自定义 keyframe，Tailwind v4 无 tailwindcss-animate，`animate-in` 类不可用）；条目两行排版（主文案 font-medium + description 次要行），hover `bg-accent`、选中 `bg-accent/60` + 右侧 Check 对勾。点外部/Esc 关闭。
- App.tsx 三处原生 select（agent/模型/授权）全部换成 ComposerDropdown，onChange 语义不变；工具栏行 gap-0.5 → gap-1；删除 ChevronDown 导入与 COMPOSER_PILL_CLASS 引用（chat-view 里仍导出，未删）。
- `npm run build` 通过；vite HMR 直接生效，无需重启桌面端。
- **双勾 bug 修复**：不同 provider 暴露相同模型 id（mimo-v2.5-pro 同时在 OpenCode Go 与小米 Token Plan），下拉项 value 用原始 id 导致两条都命中选中。修复：选项 key = `${provider.id}::${model.id}` 唯一化，`currentModelKey` 取第一个匹配（后端 preferredModelId 不带 provider 信息，无法更精确），onChange 时还原原始 id 再调 setDefaultModel。顺带消除了选项 value 重复导致的 React key 警告。

## 「设置与配对」移除「授权 / 待确认命令」区块（2026-09-12）

- 产品决定：授权模式已在 composer 工具栏有下拉入口，设置页重复 → 删除。
- App.tsx：删 SettingsView 的授权 section + props（approvalMode/pending/onSetMode/onDecide）、`pending` state、`handleDecide`、`DANGER_LABELS`/`dangerText`、侧栏设置按钮的待确认 Badge、composer 里「待确认 N → 跳设置页」按钮（失效入口）；移除 Badge/ShieldAlert/PendingApproval 等导入；保留 composer 授权下拉与 `getApprovalMode/setApprovalMode`（`refreshSecurity` 只留配对+模式）。
- api：tauri.ts 删 `getPendingApprovals`/`decideApproval`；types.ts 删 `PendingApproval`/`ApprovalReason`（`ApprovalMode` 保留）。
- Rust：lib.rs 删 Tauri 命令 `get_pending_approvals`/`decide_approval` + handler 注册 + `PendingApproval` 导入。**`approval_gate.rs` 与 HTTP `/api/approvals*` 端点保留**（iOS 端仍在用，危险命令拦截机制不动）。
- 验证：npm run build exit=0；`cargo check` 通过（无 error/无 unused warning）；dev server 自动重建成功，8787/1420 均 200。注意 `cargo build` 会因 dev 进程占用 exe 报 LNK1104，非代码问题，用 `cargo check` 或重启 dev 验证。

## 已提交推送（用户确认后）

- `00fcd95 docs(memory)` 记忆文档 → `205bda1 feat(windows)` 代码（12 files, +2662/-930），已 push 到 origin/main（main == origin/main，工作区干净）。
- 提交信息规范沿用仓库习惯：中文 `type(scope): 标题` + 分点正文 + `Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>`。
- Windows 控制台下 `git commit -m "中文"` 有编码风险，改用 `git commit -F <utf8文件>`（Write 工具写到 temp）稳妥。

## 模型偏好带上 provider（修"同名模型选不中"，用户反馈后）

- 根因：`models.json` 只存裸 modelId，同名模型（mimo-v2.5-pro 同时来自 OpenCode Go 与小米 Token Plan）提交后无法区分，回显永远勾第一个。且 opencode 的 `--model` 本来就要求 `provider/model` 复合格式，裸 id 传下去 provider 也不对。
- 修法（端到端带 provider，iOS 兼容保持）：
  - `model_prefs.rs`：`Stored` 加 `defaultProviders` 表；`set(agent, modelId, providerId)` 三参配对记录/清除；`get_provider()` 单独查。旧文件（只有 defaultModels）serde default 兼容加载（TC-MP-05）。
  - `SetModelBody` / Tauri `set_default_model` 加可选 `providerId`；回显加 `preferredProviderId`（`preferredModelId` 保持裸 id，不破坏 iOS 契约）。
  - `command_runner.rs`：仅 opencode 且 model 无 `/` 时拼 `provider/model` 传 `--model`。
  - 前端：下拉 value=`providerId::modelId`，onChange 拆开配对提交；选中判定 id+provider 精确匹配，旧记录退回第一个。
- 验证：cargo test 150 全绿（+TC-MP-04/05）；npm build 通过；tauri dev 自动重建。

## 修 Tauri invoke 参数名错位（`missing required key conversationId`）

- 根因：5 个对话命令（get/activate/set_archived/delete/toggle_pin）后端参数名 `conversation_id`，前端 invoke 传的是 `{ id }` —— Tauri 校验的是 `conversationId`，运行时报 missing key。**类型检查（tsc）抓不到 invoke 参数名错误**，只有运行时才暴露。
- 修复：`tauri.ts` 5 处改传 `{ conversationId: id }`；其余命令（send_command/set_default_model/switch_active_agent 等）参数名逐一核对无误。
- 教训：新增 Tauri 命令时，前后端参数名必须逐字对照后端签名（camelCase↔snake_case），构建通过 ≠ 可用。

## 对话界面样式对齐 + composer 目录条（用户截图参考）

- 消息区样式（对齐参考截图）：用户消息 = 右对齐 `rounded-2xl rounded-br-md` 气泡 + 上方右侧时间戳（`星期五 20:48` 格式，`createdAtMs` 转录自带）+ "我"小头像圆；助手消息 = 去掉 agentName Badge，Markdown 直接通栏（MessageList 不再收 agentName）。
- 新增 `workdir-picker.tsx`：composer 卡片上方独立目录条（`h-9 rounded-xl border`，Folder 图标 + 路径末段加粗 + "对话与文件操作将作用于所选目录"提示），点击向上展开弹层：当前目录+清除 / 主目录+盘符快捷 / 目录浏览器（点名字进入、右侧 ✓ 选定、返回上级）。ChatView 加 `composerTopBar` slot。
- 后端新增 4 个 Tauri 命令：`get_agent_workdir` / `set_agent_workdir`（validate_workdir 白名单校验）/ `browse_roots` / `browse_folder`——全部复用 `folder_browser`，无新逻辑。
- 数据流：workdir 跟随 `convAgentId`（当前对话的 agent），不是全局 activeAgentId。
- 验证：cargo test 150 全绿；npm build exit=0；桌面端自动重建在线。

## 对话历史 ↔ 工作目录关联（2026-09-12）

- 需求：对话创建时绑定目录、侧栏按目录分组、可按目录过滤、新对话可选目录（浏览/手输/最近使用/留空不绑定）、已有对话可改绑、打开历史对话恢复目录上下文。
- **后端**：
  - `conversation_store.rs`：新增 `create_with_workdir(agent, workdir)`（空串/空白=不绑定，`normalize_workdir` 归一）与 `set_workdir(conv_id, workdir)`（目录必须真实存在，否则新错误变体 `ConvError::InvalidWorkdir`→HTTP 400；不 bump updated_at，与 rename 一致）。`create(agent)` 保留原签名（13 处测试零改动）。conversation_api 的 create/patch body 均加 `workdir`。
  - `lib.rs`：`send_command` 加可选 `workdir`（草稿物化时绑定）；新 Tauri 命令 `set_conversation_workdir` 并注册。执行 cwd 生效链不变：conv.workdir_override → workdir_prefs → CLI 默认。
  - **坑**：Tauri 命令**参数上不能写 `///` 文档注释**（error: only allowed built-in attributes in function parameters），要用 `//`。
  - 测试 +TC-CV-10/11；**坑**：TC-CV-10 重载验证前必须 append 一条消息——`startup_recover` 会清空对话（TC-CV-07 既定行为），空对话"活不过"重载。
- **前端**：
  - `WorkdirPicker` 改为持久化无关（去 agentId/内部 setAgentWorkdir），新 props `recentDirs`（来自历史对话绑定，按最近活动去重排序）+ `hint`（草稿态"新对话将绑定此目录；留空 = 不绑定"）；弹层加「最近使用」chips 与手动输入路径行（回车/✓ 提交，目录不存在由后端拒绝）。
  - `App.tsx`：目录上下文三态——已有对话显示 conv.workdirOverride（权威）；草稿 `draftWorkdir: string|null|undefined`（undefined=未选择/null=明确解绑/string=创建时绑定），handleSend 仅草稿态透传 workdir，发送成功后重置。`handleSetWorkdir`：有对话→`setConversationWorkdir`+本地同步+refreshConversations；草稿→只改本地。
  - 侧栏重写为目录分组：组 key=workdirOverride??UNBOUND，组序按组内最新活动、未绑定组垫底，组内保持后端序（置顶在组首）；过滤头（全部对话/未绑定/各目录，`DirFilterMenu` 下拉），组头点击=只看该组（再点恢复全部）；打开对话/新对话落目录时 `setDirFilter` 同步（目录、当前对话、历史列表三者一致）。
- 验证：cargo test **152 全绿**；npm build exit=0；dev 自动重建在线。改动未提交，等用户确认。

## 流式回复实时渲染修复（2026-09-12 上午，续上会话）

- 根因：`command_runner.rs` 用 `cmd.output()` 一次性阻塞读取子进程 stdout，回复全部跑完才 append 转录 + emit `terminal-updated`，前端只能等"完成"信号；且事件里没有增量内容，不切会话就看不到。落盘转录本身是完整的（`conv_eaae1b2f.json` 有完整 assistant 条目）。
- 修复（端到端）：
  - 后端：`cmd.output()` → `spawn_blocking` 内 `BufReader` 逐块读 stdout（行缓冲 + ≥300ms 节流 + 末帧补发），每帧 `emit_stream_delta` 广播 `conversation-delta` 事件（payload: convId/commandId/text/final）。stderr 仍完成后一次性处理。
  - 前端 `App.tsx`：新增 `streaming` 槽位 state（非原地改 messages），监听 `conversation-delta` 且 convId 匹配当前会话才写；转录条目落库后 fetchConversation 覆盖并清空 streaming，不重复渲染。**`setActiveConvId` 全部换成 `updateActiveConvId`**（同步维护 `activeConvIdRef`，事件闭包读 ref 而非 setState updater，React 18 严格模式下 updater 不可靠）。
  - 修复后 `cargo test` **154 全绿**（+2 项流式回归：TC-CR-stream-01/02，用慢速 echo 假 CLI 证明完成前就有 delta 事件）。
- **验证链路重要教训**：vite HMR 只热更前端；Rust 改动必须重启 tauri dev 才进二进制。上次会话疑似"事件没到前端"其实是旧 exe。验证方法：读 `target\debug\brewping-desktop.exe` 二进制 grep 特征串（如 `conversation-delta`）。
- UI 顶栏遗留的 TEMP-DEBUG 事件计数 span 已删（`dbgEvents` 变量早已不在，span 是孤儿引用——tsc 竟不报？实际是 Edit 假成功导致该 span 引用了不存在的变量，删除前 build 是过的，说明该 span 所在编辑当时也没落盘，之后才补上——编辑后必须复核）。
- 2026-09-12 09:18 重建重启：kill 旧实例 → npm build exit=0 → tauri dev（WEBVIEW2 --no-sandbox）→ /api/status 200，exe 含 conversation-delta，PID 28792。

## 对话 ↔ 目录一对一绑定补全（2026-09-12 上午，用户反馈后）

- 用户反馈：切对话目录不变、所有对话共用一个目录。排查结论：**后端绑定架构完好**（workdirOverride/创建绑定/改绑都在），问题在前端两处断点：
  1. `handleSend` 草稿态 `draftWorkdir ?? null` —— 用户没手动选目录时新对话**创建后不绑定**（workdirOverride=null），全都落到"未绑定"。
  2. `effectiveWorkdir` 对未绑定对话返回 `null`（显示"未设置"），与注释和执行回落链（conv 绑定 → agent 偏好 → CLI 默认）不一致——目录条不反映真实生效目录。
- 修复（纯前端 App.tsx，零后端改动）：
  - 新对话创建即记录"当时的目录"：`bindWorkdir = draftWorkdir !== undefined ? draftWorkdir : (agentWorkdir ?? null)`（显式清除=null 仍不绑定）。
  - 未绑定对话显示回落 `agentWorkdir`：`activeConv?.workdirOverride ?? agentWorkdir`。
  - hint 文案同步："新对话将记录此目录（可另选或清除后不绑定）" / "本对话绑定的工作目录；更改只影响当前对话"。
- npm build exit=0；HMR 生效无需重启（纯前端）。旧对话仍为未绑定（不做静默迁移，用户可在目录条手动改绑，改绑后进对应分组）。

## 清空对话历史供用户重测（2026-09-12 13:19）

- 用户要求清空对话历史从头测试：`~/.brewping/conversations/` 5 个文件（4 条 conv_*.json + index.json）**移动备份**到 `C:\Users\czk\.brewping\backup\conversations-20260912-131914\`（未删除，可恢复）。
- 保留 pairing.json / models.json / approval.json / workdirs.json / device.json（非对话数据，清了要重配对/丢偏好）。
- 重启桌面端加载空状态（新 PID 19172，task swyvn2），8787 在线、conversations 目录 0 文件。

## 修侧栏目录过滤弹层被裁切（2026-09-12 17:15，用户截图反馈）

- 现象：DirFilterMenu 弹层左侧被窗口边缘裁掉一截（"按目录"组头等显示不全）。
- 根因：侧栏仅 `w-52`（208px），弹层 `absolute right-0 w-56`（224px）右对齐到触发按钮后向左展开超出窗口左缘；且外层 `overflow-y-auto` 滚动容器会连带水平裁剪绝对定位子元素，改 `left-0` 也躲不开。
- 修复：**portal 到 body + fixed 定位**——`createPortal(document.body)`，`toggle` 时用 `getBoundingClientRect()` 算坐标（左对齐按钮；`min(r.left, innerWidth-224-8)` 防右溢出；`max(8,·)` 贴左保护），弹层 `max-h-[60vh] overflow-y-auto` 防底部溢出；点外关闭的判断须同时排除 `popRef`（portal 后弹层不在 rootRef 内，否则点条目会先关菜单）；补 Esc 关闭。样式复用 `composer-popup-in` 动画与圆角卡片阴影。
- 教训：**侧栏/滚动容器内的下拉弹层不要用 absolute**（窗口边缘 + overflow 双重裁剪），一律 portal + fixed。
- npm build exit=0；HMR 生效（纯前端）。

## 侧栏目录组去"下钻感"（2026-09-12 17:20，用户反馈后）

- 用户反馈：点目录组头后整个列表只剩该目录（像下钻导航），其他文件夹的对话消失。
- 交互重定（App.tsx）：
  - **组头点击 = 折叠/展开**（`collapsedGroups` state + ChevronDown 旋转指示），所有目录组始终可见；数量徽标保留。
  - **删除两处隐式 setDirFilter 同步**：handleSend（新对话落目录后）与 handleOpenConversation（打开历史对话时）——它们会把列表突然切成单目录视野，是下钻感的另一半来源。打开对话只切当前对话，目录条仍随 effectiveWorkdir 自动显示绑定目录。
  - **按目录筛选只剩一条显式路径**：右上角文件夹图标下拉（DirFilterMenu），过滤头标签随之变化。
- 教训：列表视野的变更必须是用户显式动作（菜单选择/折叠），随数据变化隐式改视野（打开对话/新建对话同步过滤）会被感知为"导航走丢了"。
- npm build exit=0；HMR 生效。

## 待办

- UI 层面的多对话交互（切窗口/新建/归档/置顶/草稿独立性）需用户在桌面窗口实际操作验收。
- 模型 provider + invoke 参数名修复 + 目录条/样式 + 目录关联改动均未提交，等用户确认。
